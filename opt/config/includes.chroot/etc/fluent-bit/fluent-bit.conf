# DebVisor default Fluent Bit configuration
#
# This main config is intentionally minimal. It expects one or more
# additional configs to be present under /etc/fluent-bit/ that define
# actual inputs/filters/outputs.

[SERVICE]
    Flush        5
    Daemon       Off
    Log_Level    info

# Include DebVisor compliance pipeline if present
@INCLUDE debvisor-compliance.conf
