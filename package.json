{
  "name": "debvisor-ci",
  "version": "0.0.0",
  "private": true,
  "description": "Development and CI tooling for DebVisor (commitlint)",
  "license": "UNLICENSED",
  "devDependencies": {
    "@commitlint/cli": "^18.0.0",
    "@commitlint/config-conventional": "^18.0.0"
  }
}
