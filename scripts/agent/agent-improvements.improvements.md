# Improvements: `agent-improvements.py`

## Suggested improvements
- Consider documenting class construction/expected invariants.
- Consider validating the input file name ends with `.improvements.md` to avoid accidental edits.
- Consider adding CLI args to select AI backend (`--backend`) and print diagnostics (`--describe-backends`).

## Notes
- These are suggestions based on static inspection; validate behavior with tests/runs.
- File: `scripts/agent/agent-improvements.py`
