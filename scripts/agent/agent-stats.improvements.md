# Improvements: `agent-stats.py`

## Fixed
- Add support for exporting stats to CSV.
- Add logging for all major actions.
- Add type hints for all methods.

## Suggested improvements
- Add trend analysis (compare with previous run).
- Visualize stats using a simple CLI graph (e.g., ASCII bars).
- Track code coverage metrics if available.
- Add docstrings for all methods.
- Add unit tests for edge cases.
- Use `pathlib` consistently.

## Notes
- These are suggestions based on static inspection; validate behavior with tests/runs.
- File: `scripts/agent/agent-stats.py`