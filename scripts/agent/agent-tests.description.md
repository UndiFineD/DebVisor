● I'll examine the current code and update the description file accordingly.

● Let me search for these files:

● Now I'll update the description file with accurate information based on the current code:

● I don't have write permissions for this file. Let me check the file permissions and structure:

● I apologize, but I don't have the necessary permissions to modify the
   `agent-tests.description.md` file or run shell commands in this environment. Based on my
   analysis of the current code in `agent-tests.py`, here's what should be updated:

   ## Recommended Content for `agent-tests.description.md`:

   The file should be completely rewritten to accurately reflect the current implementation:

   **Key points that need to be documented:**

   1. **Syntax Validation**: Uses `ast.parse()` to validate Python syntax before accepting AI
   changes
   2. **Error Recovery**: Automatically reverts to previous content if validation fails
   3. **Raw Python Output**: Writes Python without markdown post-processing
   4. **Default Content**: Provides pytest skeleton with `import pytest`
   5. **CLI Arguments**:
      - `--context` (required): Path to test file
      - `--prompt` (required): Improvement instructions
      - `--backend`, `--describe-backends`, `-v/--verbose` (optional)
   6. **Integration**: Called by `agent.py` for test suite improvements

   **To complete this task, you'll need to:**
   - Manually update the file with proper markdown documentation
   - Replace the current bullet-point narrative with structured documentation
   - Include sections for Overview, Key Features, CLI Interface, Integration, Usage Example, and
   Technical Details

   Would you like me to provide the complete formatted markdown content that should replace the
   current file contents?
