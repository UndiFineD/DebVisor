# Code Issues Report: usr\DebVisor\bin\debvisor-vnc-console-enhanced.sh
Generated: 2025-12-13T15:25:25.617217
Source: usr\DebVisor\bin\debvisor-vnc-console-enhanced.sh

## Issues Summary
Total: 690 issues found

| Line | Column | Tool | Code | Severity | Message |
|------|--------|------|------|----------|---------|
| 1 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 2 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 3 | 66 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 4 | 67 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 5 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 6 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 7 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 8 | 68 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 9 | 75 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 10 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 11 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 12 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 13 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 14 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 15 | 66 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 16 | 67 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 17 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 18 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 19 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 20 | 68 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 21 | 75 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 22 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 23 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 24 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 25 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 26 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 27 | 66 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 28 | 67 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 29 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 30 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 31 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 32 | 68 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 33 | 75 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 34 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 35 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 36 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 37 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 38 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 39 | 66 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 40 | 67 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 41 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 42 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 43 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 44 | 68 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 45 | 75 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 46 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 47 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 48 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 49 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 50 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 51 | 66 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 52 | 67 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 53 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 54 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 55 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 56 | 68 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 57 | 75 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 58 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 59 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 60 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 61 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 62 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 63 | 66 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 64 | 67 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 65 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 66 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 67 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 68 | 68 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 69 | 75 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 70 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 71 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 72 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 73 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 74 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 75 | 66 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 76 | 67 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 77 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 78 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 79 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 80 | 68 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 81 | 75 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 82 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 83 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 84 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 85 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 86 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 87 | 66 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 88 | 67 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 89 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 90 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 91 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 92 | 68 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 93 | 75 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 94 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 95 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 96 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 97 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 98 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 99 | 66 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 100 | 67 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 101 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 102 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 103 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 104 | 68 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 105 | 75 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 106 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 107 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 108 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 109 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 110 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 111 | 66 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 112 | 67 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 113 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 114 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 115 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 116 | 68 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 117 | 75 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 118 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 119 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 120 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 121 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 122 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 123 | 66 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 124 | 67 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 125 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 126 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 127 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 128 | 68 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 129 | 75 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 130 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 131 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 132 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 133 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 134 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 135 | 66 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 136 | 67 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 137 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 138 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 139 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 140 | 68 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 141 | 75 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 142 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 143 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 144 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 145 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 146 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 147 | 66 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 148 | 67 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 149 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 150 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 151 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 152 | 68 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 153 | 75 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 154 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 155 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 156 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 157 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 158 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 159 | 66 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 160 | 67 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 161 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 162 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 163 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 164 | 68 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 165 | 75 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 166 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 167 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 168 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 169 | 80 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 170 | 35 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 171 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 172 | 20 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 173 | 80 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 174 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 175 | 19 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 176 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 177 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 178 | 23 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 179 | 31 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 180 | 45 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 181 | 48 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 182 | 31 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 183 | 40 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 184 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 185 | 20 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 186 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 187 | 24 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 188 | 24 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 189 | 36 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 190 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 191 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 192 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 193 | 9 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 194 | 17 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 195 | 19 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 196 | 20 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 197 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 198 | 13 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 199 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 200 | 17 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 201 | 36 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 202 | 20 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 203 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 204 | 80 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 205 | 28 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 206 | 80 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 207 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 208 | 13 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 209 | 57 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 210 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 211 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 212 | 16 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 213 | 61 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 214 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 215 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 216 | 13 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 217 | 59 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 218 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 219 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 220 | 14 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 221 | 57 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 222 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 223 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 224 | 20 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 225 | 22 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 226 | 23 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 227 | 22 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 228 | 27 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 229 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 230 | 38 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 231 | 48 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 232 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 233 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 234 | 39 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 235 | 94 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 236 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 237 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 238 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 239 | 23 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 240 | 25 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 241 | 77 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 242 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 243 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 244 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 245 | 34 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 246 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 247 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 248 | 14 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 249 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 250 | 41 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 251 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 252 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 253 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 254 | 80 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 255 | 20 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 256 | 80 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 257 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 258 | 16 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 259 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 294 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 295 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 296 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 297 | 48 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 298 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 299 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 300 | 19 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 301 | 45 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 302 | 16 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 303 | 80 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 304 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 305 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 306 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 307 | 21 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 308 | 23 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 309 | 53 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 310 | 46 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 311 | 17 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 312 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 313 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 314 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 315 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 316 | 20 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 317 | 41 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 318 | 40 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 319 | 17 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 320 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 321 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 322 | 45 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 323 | 17 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 324 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 325 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 326 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 327 | 80 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 328 | 27 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 329 | 80 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 330 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 331 | 20 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 332 | 23 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 333 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 334 | 50 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 335 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 336 | 44 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 337 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 338 | 52 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 339 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 340 | 57 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 341 | 47 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 342 | 77 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 343 | 21 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 344 | 11 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 345 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 346 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 347 | 41 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 348 | 82 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 349 | 55 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 350 | 9 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 351 | 48 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 352 | 44 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 353 | 60 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 354 | 61 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 355 | 11 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 356 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 357 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 358 | 30 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 359 | 19 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 360 | 52 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 361 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 362 | 55 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 363 | 72 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 364 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 365 | 53 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 366 | 19 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 373 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 374 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 375 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 376 | 28 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 377 | 59 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 378 | 55 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 379 | 25 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 380 | 21 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 381 | 11 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 382 | 9 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 383 | 39 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 384 | 13 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 385 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 386 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 387 | 27 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 388 | 25 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 389 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 390 | 45 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 391 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 392 | 52 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 393 | 57 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 394 | 17 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 395 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 396 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 397 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 398 | 34 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 399 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 400 | 38 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 401 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 402 | 48 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 403 | 74 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 404 | 57 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 405 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 406 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 407 | 24 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 408 | 73 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 409 | 51 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 410 | 74 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 411 | 17 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 412 | 9 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 413 | 53 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 414 | 75 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 415 | 17 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 416 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 417 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 418 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 419 | 80 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 420 | 27 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 421 | 80 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 422 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 423 | 27 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 424 | 23 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 425 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 426 | 57 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 427 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 428 | 44 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 429 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 430 | 16 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 431 | 28 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 432 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 433 | 58 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 434 | 40 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 435 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 436 | 26 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 437 | 69 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 438 | 52 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 439 | 17 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 440 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 441 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 442 | 40 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 443 | 36 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 444 | 30 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 445 | 53 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 446 | 35 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 457 | 32 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 458 | 81 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 459 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 460 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 461 | 55 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 462 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 463 | 25 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 464 | 29 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 465 | 38 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 466 | 44 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 467 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 468 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 469 | 54 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 470 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 471 | 53 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 472 | 19 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 481 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 482 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 483 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 484 | 27 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 485 | 21 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 486 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 487 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 488 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 489 | 38 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 490 | 44 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 491 | 17 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 492 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 493 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 494 | 32 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 495 | 72 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 496 | 41 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 497 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 498 | 51 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 499 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 500 | 28 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 501 | 17 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 502 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 503 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 504 | 41 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 505 | 13 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 506 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 507 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 508 | 80 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 509 | 21 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 510 | 80 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 511 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 512 | 26 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 513 | 40 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 514 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 515 | 37 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 516 | 38 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 517 | 17 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 518 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 519 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 520 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 521 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 522 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 523 | 54 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 524 | 54 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 525 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 526 | 47 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 527 | 40 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 528 | 24 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 529 | 74 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 530 | 76 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 531 | 80 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 532 | 80 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 533 | 75 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 534 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 535 | 88 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 536 | 11 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 537 | 9 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 538 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 539 | 28 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 540 | 38 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 541 | 9 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 542 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 543 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 544 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 545 | 53 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 546 | 17 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 547 | 25 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 548 | 51 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 549 | 44 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 550 | 51 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 551 | 29 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 552 | 19 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 553 | 34 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 554 | 28 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 555 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 556 | 13 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 557 | 17 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 558 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 559 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 560 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 561 | 26 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 562 | 21 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 563 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 564 | 47 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 565 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 566 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 567 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 568 | 38 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 569 | 46 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 570 | 17 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 571 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 572 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 573 | 40 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 574 | 28 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 575 | 45 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 576 | 59 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 577 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 578 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 579 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 580 | 80 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 581 | 21 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 582 | 80 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 583 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 584 | 27 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 585 | 23 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 586 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 587 | 57 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 588 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 589 | 44 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 590 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 591 | 31 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 592 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 593 | 51 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 594 | 58 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 595 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 596 | 51 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 597 | 48 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 598 | 51 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 599 | 58 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 600 | 41 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 601 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 602 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 603 | 62 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 604 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 605 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 606 | 29 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 607 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 608 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 609 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 610 | 59 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 611 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 612 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 613 | 26 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 614 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 615 | 46 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 616 | 48 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 617 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 618 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 619 | 36 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 620 | 29 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 621 | 77 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 622 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 623 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 624 | 32 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 625 | 36 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 626 | 35 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 627 | 85 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 628 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 629 | 44 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 630 | 9 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 631 | 28 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 632 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 633 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 634 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 635 | 22 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 636 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 637 | 35 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 638 | 30 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 639 | 9 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 640 | 38 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 641 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 642 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 643 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 644 | 31 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 645 | 51 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 646 | 50 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 647 | 54 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 648 | 59 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 649 | 58 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 650 | 55 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 651 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 652 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 653 | 57 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 654 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 655 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 656 | 80 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 657 | 16 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 658 | 80 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 659 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 660 | 9 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 661 | 38 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 662 | 22 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 663 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 664 | 28 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 665 | 20 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 666 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 667 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 668 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 669 | 23 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 670 | 10 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 671 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 672 | 23 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 673 | 21 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 674 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 675 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 676 | 24 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 677 | 35 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 678 | 48 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 679 | 41 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 680 | 45 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 681 | 23 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 682 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 683 | 60 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 684 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 685 | 22 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 686 | 37 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 687 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 688 | 47 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 689 | 23 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 690 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 691 | 48 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 692 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 693 | 23 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 694 | 34 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 695 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 696 | 23 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 697 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 698 | 39 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 699 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 700 | 23 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 701 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 702 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 703 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 704 | 16 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 705 | 40 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 706 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 707 | 25 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 708 | 37 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 709 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 710 | 19 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 711 | 24 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 712 | 19 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 713 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 714 | 19 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 715 | 26 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 716 | 19 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 717 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 718 | 11 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 719 | 50 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 720 | 24 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 721 | 19 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 722 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 723 | 9 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 724 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 725 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 726 | 25 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 727 | 17 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 728 | 19 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 729 | 27 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 730 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 731 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 732 | 19 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 733 | 27 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 734 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 735 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 736 | 16 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 737 | 31 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 738 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 739 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 740 | 11 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 741 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 742 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 743 | 9 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 744 | 5 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 745 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 746 | 45 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 747 | 14 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 748 | 3 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |

## Implementation Status
Items marked below as fixed:


## Fix Proposals

**690 issues to fix:**


### Issue at Line 1

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 2

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 3

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 4

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 5

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 6

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 7

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 8

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 9

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 10

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 11

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 12

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 13

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 14

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 15

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 16

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 17

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 18

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 19

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 20

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 21

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 22

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 23

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 24

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 25

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 26

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 27

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 28

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 29

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 30

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 31

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 32

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 33

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 34

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 35

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 36

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 37

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 38

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 39

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 40

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 41

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 42

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 43

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 44

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 45

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 46

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 47

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 48

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 49

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 50

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 51

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 52

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 53

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 54

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 55

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 56

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 57

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 58

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 59

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 60

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 61

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 62

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 63

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 64

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 65

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 66

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 67

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 68

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 69

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 70

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 71

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 72

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 73

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 74

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 75

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 76

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 77

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 78

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 79

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 80

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 81

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 82

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 83

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 84

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 85

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 86

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 87

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 88

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 89

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 90

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 91

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 92

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 93

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 94

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 95

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 96

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 97

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 98

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 99

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 100

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 101

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 102

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 103

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 104

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 105

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 106

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 107

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 108

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 109

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 110

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 111

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 112

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 113

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 114

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 115

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 116

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 117

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 118

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 119

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 120

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 121

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 122

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 123

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 124

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 125

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 126

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 127

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 128

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 129

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 130

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 131

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 132

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 133

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 134

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 135

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 136

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 137

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 138

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 139

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 140

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 141

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 142

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 143

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 144

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 145

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 146

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 147

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 148

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 149

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 150

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 151

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 152

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 153

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 154

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 155

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 156

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 157

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# See the License for the specific language governing permissions and
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 158

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# limitations under the License.

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 159

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 160

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#!/bin/bash
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 161

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Copyright (c) 2025 DebVisor contributors
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 162

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 163

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 164

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 165

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 166

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

###############################################################################
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 167

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

###############################################################################
# debvisor-vnc-console-enhanced.sh
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 168

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

###############################################################################
# debvisor-vnc-console-enhanced.sh
# Enhanced VNC and console tools with validation, security hardening,
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 169

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# See the License for the specific language governing permissions and
# limitations under the License.

###############################################################################
# debvisor-vnc-console-enhanced.sh
# Enhanced VNC and console tools with validation, security hardening,
# and audit logging
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 170

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# limitations under the License.

###############################################################################
# debvisor-vnc-console-enhanced.sh
# Enhanced VNC and console tools with validation, security hardening,
# and audit logging
###############################################################################
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 171

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

###############################################################################
# debvisor-vnc-console-enhanced.sh
# Enhanced VNC and console tools with validation, security hardening,
# and audit logging
###############################################################################

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 172

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
###############################################################################
# debvisor-vnc-console-enhanced.sh
# Enhanced VNC and console tools with validation, security hardening,
# and audit logging
###############################################################################

set -eEuo pipefail
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 173

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# debvisor-vnc-console-enhanced.sh
# Enhanced VNC and console tools with validation, security hardening,
# and audit logging
###############################################################################

set -eEuo pipefail

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 174

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Enhanced VNC and console tools with validation, security hardening,
# and audit logging
###############################################################################

set -eEuo pipefail

# Script metadata
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 175

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# and audit logging
###############################################################################

set -eEuo pipefail

# Script metadata
SCRIPT_VERSION="1.0.0"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 176

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
###############################################################################

set -eEuo pipefail

# Script metadata
SCRIPT_VERSION="1.0.0"
SCRIPT_NAME="$(basename "$0")"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 177

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

set -eEuo pipefail

# Script metadata
SCRIPT_VERSION="1.0.0"
SCRIPT_NAME="$(basename "$0")"
LOG_FILE="/var/log/debvisor-vnc-console.log"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 178

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
set -eEuo pipefail

# Script metadata
SCRIPT_VERSION="1.0.0"
SCRIPT_NAME="$(basename "$0")"
LOG_FILE="/var/log/debvisor-vnc-console.log"
AUDIT_LOG="/var/log/debvisor-console-audit.log"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 179

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

# Script metadata
SCRIPT_VERSION="1.0.0"
SCRIPT_NAME="$(basename "$0")"
LOG_FILE="/var/log/debvisor-vnc-console.log"
AUDIT_LOG="/var/log/debvisor-console-audit.log"
CONFIG_DIR="/etc/debvisor/vnc"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 180

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Script metadata
SCRIPT_VERSION="1.0.0"
SCRIPT_NAME="$(basename "$0")"
LOG_FILE="/var/log/debvisor-vnc-console.log"
AUDIT_LOG="/var/log/debvisor-console-audit.log"
CONFIG_DIR="/etc/debvisor/vnc"
STATE_DIR="/var/lib/debvisor/vnc-state"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 181

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
SCRIPT_VERSION="1.0.0"
SCRIPT_NAME="$(basename "$0")"
LOG_FILE="/var/log/debvisor-vnc-console.log"
AUDIT_LOG="/var/log/debvisor-console-audit.log"
CONFIG_DIR="/etc/debvisor/vnc"
STATE_DIR="/var/lib/debvisor/vnc-state"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 182

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
SCRIPT_NAME="$(basename "$0")"
LOG_FILE="/var/log/debvisor-vnc-console.log"
AUDIT_LOG="/var/log/debvisor-console-audit.log"
CONFIG_DIR="/etc/debvisor/vnc"
STATE_DIR="/var/lib/debvisor/vnc-state"

# Security settings
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 183

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
LOG_FILE="/var/log/debvisor-vnc-console.log"
AUDIT_LOG="/var/log/debvisor-console-audit.log"
CONFIG_DIR="/etc/debvisor/vnc"
STATE_DIR="/var/lib/debvisor/vnc-state"

# Security settings
CONSOLE_TOKEN_TTL=3600  # 1 hour
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 184

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
AUDIT_LOG="/var/log/debvisor-console-audit.log"
CONFIG_DIR="/etc/debvisor/vnc"
STATE_DIR="/var/lib/debvisor/vnc-state"

# Security settings
CONSOLE_TOKEN_TTL=3600  # 1 hour
CONSOLE_TOKEN_LENGTH=32
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 185

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
CONFIG_DIR="/etc/debvisor/vnc"
STATE_DIR="/var/lib/debvisor/vnc-state"

# Security settings
CONSOLE_TOKEN_TTL=3600  # 1 hour
CONSOLE_TOKEN_LENGTH=32
MAX_CONSOLE_SESSIONS=10
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 186

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
STATE_DIR="/var/lib/debvisor/vnc-state"

# Security settings
CONSOLE_TOKEN_TTL=3600  # 1 hour
CONSOLE_TOKEN_LENGTH=32
MAX_CONSOLE_SESSIONS=10
ALLOWED_PORTS=(5900-5999 6000-6099)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 187

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

# Security settings
CONSOLE_TOKEN_TTL=3600  # 1 hour
CONSOLE_TOKEN_LENGTH=32
MAX_CONSOLE_SESSIONS=10
ALLOWED_PORTS=(5900-5999 6000-6099)
MIN_VNC_PORT=5900
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 188

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Security settings
CONSOLE_TOKEN_TTL=3600  # 1 hour
CONSOLE_TOKEN_LENGTH=32
MAX_CONSOLE_SESSIONS=10
ALLOWED_PORTS=(5900-5999 6000-6099)
MIN_VNC_PORT=5900
MAX_VNC_PORT=5999
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 189

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
CONSOLE_TOKEN_TTL=3600  # 1 hour
CONSOLE_TOKEN_LENGTH=32
MAX_CONSOLE_SESSIONS=10
ALLOWED_PORTS=(5900-5999 6000-6099)
MIN_VNC_PORT=5900
MAX_VNC_PORT=5999

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 190

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
CONSOLE_TOKEN_LENGTH=32
MAX_CONSOLE_SESSIONS=10
ALLOWED_PORTS=(5900-5999 6000-6099)
MIN_VNC_PORT=5900
MAX_VNC_PORT=5999

# Colors
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 191

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
MAX_CONSOLE_SESSIONS=10
ALLOWED_PORTS=(5900-5999 6000-6099)
MIN_VNC_PORT=5900
MAX_VNC_PORT=5999

# Colors
RED='\033[0;31m'
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 192

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
ALLOWED_PORTS=(5900-5999 6000-6099)
MIN_VNC_PORT=5900
MAX_VNC_PORT=5999

# Colors
RED='\033[0;31m'
GREEN='\033[0;32m'
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 193

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
MIN_VNC_PORT=5900
MAX_VNC_PORT=5999

# Colors
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 194

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
MAX_VNC_PORT=5999

# Colors
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 195

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

# Colors
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m'
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 196

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Colors
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m'

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 197

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m'

# Error handling
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 198

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m'

# Error handling
trap 'error_handler $? $LINENO' ERR
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 199

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m'

# Error handling
trap 'error_handler $? $LINENO' ERR
trap 'cleanup' EXIT
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 200

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
BLUE='\033[0;34m'
NC='\033[0m'

# Error handling
trap 'error_handler $? $LINENO' ERR
trap 'cleanup' EXIT

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 201

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
NC='\033[0m'

# Error handling
trap 'error_handler $? $LINENO' ERR
trap 'cleanup' EXIT

###############################################################################
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 202

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

# Error handling
trap 'error_handler $? $LINENO' ERR
trap 'cleanup' EXIT

###############################################################################
# Logging & Audit Functions
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 203

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Error handling
trap 'error_handler $? $LINENO' ERR
trap 'cleanup' EXIT

###############################################################################
# Logging & Audit Functions
###############################################################################
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 204

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
trap 'error_handler $? $LINENO' ERR
trap 'cleanup' EXIT

###############################################################################
# Logging & Audit Functions
###############################################################################

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 205

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
trap 'cleanup' EXIT

###############################################################################
# Logging & Audit Functions
###############################################################################

log_info() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 206

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

###############################################################################
# Logging & Audit Functions
###############################################################################

log_info() {
    echo -e "${BLUE}[INFO]${NC} $1" | tee -a "$LOG_FILE"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 207

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
###############################################################################
# Logging & Audit Functions
###############################################################################

log_info() {
    echo -e "${BLUE}[INFO]${NC} $1" | tee -a "$LOG_FILE"
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 208

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Logging & Audit Functions
###############################################################################

log_info() {
    echo -e "${BLUE}[INFO]${NC} $1" | tee -a "$LOG_FILE"
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 209

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
###############################################################################

log_info() {
    echo -e "${BLUE}[INFO]${NC} $1" | tee -a "$LOG_FILE"
}

log_success() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 210

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

log_info() {
    echo -e "${BLUE}[INFO]${NC} $1" | tee -a "$LOG_FILE"
}

log_success() {
    echo -e "${GREEN}[SUCCESS]${NC} $1" | tee -a "$LOG_FILE"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 211

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
log_info() {
    echo -e "${BLUE}[INFO]${NC} $1" | tee -a "$LOG_FILE"
}

log_success() {
    echo -e "${GREEN}[SUCCESS]${NC} $1" | tee -a "$LOG_FILE"
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 212

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo -e "${BLUE}[INFO]${NC} $1" | tee -a "$LOG_FILE"
}

log_success() {
    echo -e "${GREEN}[SUCCESS]${NC} $1" | tee -a "$LOG_FILE"
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 213

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

log_success() {
    echo -e "${GREEN}[SUCCESS]${NC} $1" | tee -a "$LOG_FILE"
}

log_warn() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 214

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

log_success() {
    echo -e "${GREEN}[SUCCESS]${NC} $1" | tee -a "$LOG_FILE"
}

log_warn() {
    echo -e "${YELLOW}[WARN]${NC} $1" | tee -a "$LOG_FILE"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 215

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
log_success() {
    echo -e "${GREEN}[SUCCESS]${NC} $1" | tee -a "$LOG_FILE"
}

log_warn() {
    echo -e "${YELLOW}[WARN]${NC} $1" | tee -a "$LOG_FILE"
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 216

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo -e "${GREEN}[SUCCESS]${NC} $1" | tee -a "$LOG_FILE"
}

log_warn() {
    echo -e "${YELLOW}[WARN]${NC} $1" | tee -a "$LOG_FILE"
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 217

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

log_warn() {
    echo -e "${YELLOW}[WARN]${NC} $1" | tee -a "$LOG_FILE"
}

log_error() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 218

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

log_warn() {
    echo -e "${YELLOW}[WARN]${NC} $1" | tee -a "$LOG_FILE"
}

log_error() {
    echo -e "${RED}[ERROR]${NC} $1" | tee -a "$LOG_FILE"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 219

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
log_warn() {
    echo -e "${YELLOW}[WARN]${NC} $1" | tee -a "$LOG_FILE"
}

log_error() {
    echo -e "${RED}[ERROR]${NC} $1" | tee -a "$LOG_FILE"
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 220

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo -e "${YELLOW}[WARN]${NC} $1" | tee -a "$LOG_FILE"
}

log_error() {
    echo -e "${RED}[ERROR]${NC} $1" | tee -a "$LOG_FILE"
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 221

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

log_error() {
    echo -e "${RED}[ERROR]${NC} $1" | tee -a "$LOG_FILE"
}

audit_log_entry() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 222

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

log_error() {
    echo -e "${RED}[ERROR]${NC} $1" | tee -a "$LOG_FILE"
}

audit_log_entry() {
    local action="$1"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 223

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
log_error() {
    echo -e "${RED}[ERROR]${NC} $1" | tee -a "$LOG_FILE"
}

audit_log_entry() {
    local action="$1"
    local vm_name="$2"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 224

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo -e "${RED}[ERROR]${NC} $1" | tee -a "$LOG_FILE"
}

audit_log_entry() {
    local action="$1"
    local vm_name="$2"
    local result="$3"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 225

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

audit_log_entry() {
    local action="$1"
    local vm_name="$2"
    local result="$3"
    local details="${4:-}"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 226

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

audit_log_entry() {
    local action="$1"
    local vm_name="$2"
    local result="$3"
    local details="${4:-}"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 227

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
audit_log_entry() {
    local action="$1"
    local vm_name="$2"
    local result="$3"
    local details="${4:-}"

    local timestamp=$(date -Iseconds)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 228

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local action="$1"
    local vm_name="$2"
    local result="$3"
    local details="${4:-}"

    local timestamp=$(date -Iseconds)
    local user="${SUDO_USER:-${USER:-unknown}}"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 229

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local vm_name="$2"
    local result="$3"
    local details="${4:-}"

    local timestamp=$(date -Iseconds)
    local user="${SUDO_USER:-${USER:-unknown}}"
    local ip="${SSH_CLIENT%% *}"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 230

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local result="$3"
    local details="${4:-}"

    local timestamp=$(date -Iseconds)
    local user="${SUDO_USER:-${USER:-unknown}}"
    local ip="${SSH_CLIENT%% *}"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 231

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local details="${4:-}"

    local timestamp=$(date -Iseconds)
    local user="${SUDO_USER:-${USER:-unknown}}"
    local ip="${SSH_CLIENT%% *}"

    mkdir -p "$(dirname "$AUDIT_LOG")"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 232

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    local timestamp=$(date -Iseconds)
    local user="${SUDO_USER:-${USER:-unknown}}"
    local ip="${SSH_CLIENT%% *}"

    mkdir -p "$(dirname "$AUDIT_LOG")"
    echo "$timestamp | $user | $action | $vm_name | $result | $ip | $details" >> "$AUDIT_LOG"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 233

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local timestamp=$(date -Iseconds)
    local user="${SUDO_USER:-${USER:-unknown}}"
    local ip="${SSH_CLIENT%% *}"

    mkdir -p "$(dirname "$AUDIT_LOG")"
    echo "$timestamp | $user | $action | $vm_name | $result | $ip | $details" >> "$AUDIT_LOG"
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 234

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local user="${SUDO_USER:-${USER:-unknown}}"
    local ip="${SSH_CLIENT%% *}"

    mkdir -p "$(dirname "$AUDIT_LOG")"
    echo "$timestamp | $user | $action | $vm_name | $result | $ip | $details" >> "$AUDIT_LOG"
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 235

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local ip="${SSH_CLIENT%% *}"

    mkdir -p "$(dirname "$AUDIT_LOG")"
    echo "$timestamp | $user | $action | $vm_name | $result | $ip | $details" >> "$AUDIT_LOG"
}

error_handler() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 236

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    mkdir -p "$(dirname "$AUDIT_LOG")"
    echo "$timestamp | $user | $action | $vm_name | $result | $ip | $details" >> "$AUDIT_LOG"
}

error_handler() {
    local exit_code=$1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 237

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    mkdir -p "$(dirname "$AUDIT_LOG")"
    echo "$timestamp | $user | $action | $vm_name | $result | $ip | $details" >> "$AUDIT_LOG"
}

error_handler() {
    local exit_code=$1
    local line_number=$2
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 238

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "$timestamp | $user | $action | $vm_name | $result | $ip | $details" >> "$AUDIT_LOG"
}

error_handler() {
    local exit_code=$1
    local line_number=$2
    log_error "Script failed at line $line_number with exit code $exit_code"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 239

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

error_handler() {
    local exit_code=$1
    local line_number=$2
    log_error "Script failed at line $line_number with exit code $exit_code"
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 240

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

error_handler() {
    local exit_code=$1
    local line_number=$2
    log_error "Script failed at line $line_number with exit code $exit_code"
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 241

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
error_handler() {
    local exit_code=$1
    local line_number=$2
    log_error "Script failed at line $line_number with exit code $exit_code"
}

cleanup() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 242

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local exit_code=$1
    local line_number=$2
    log_error "Script failed at line $line_number with exit code $exit_code"
}

cleanup() {
    debug_log "Cleanup completed"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 243

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local line_number=$2
    log_error "Script failed at line $line_number with exit code $exit_code"
}

cleanup() {
    debug_log "Cleanup completed"
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 244

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    log_error "Script failed at line $line_number with exit code $exit_code"
}

cleanup() {
    debug_log "Cleanup completed"
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 245

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

cleanup() {
    debug_log "Cleanup completed"
}

debug_log() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 246

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

cleanup() {
    debug_log "Cleanup completed"
}

debug_log() {
    if [[ "${VERBOSE:-false}" == "true" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 247

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
cleanup() {
    debug_log "Cleanup completed"
}

debug_log() {
    if [[ "${VERBOSE:-false}" == "true" ]]; then
        echo "[DEBUG] $1" >> "$LOG_FILE"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 248

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    debug_log "Cleanup completed"
}

debug_log() {
    if [[ "${VERBOSE:-false}" == "true" ]]; then
        echo "[DEBUG] $1" >> "$LOG_FILE"
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 249

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

debug_log() {
    if [[ "${VERBOSE:-false}" == "true" ]]; then
        echo "[DEBUG] $1" >> "$LOG_FILE"
    fi
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 250

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

debug_log() {
    if [[ "${VERBOSE:-false}" == "true" ]]; then
        echo "[DEBUG] $1" >> "$LOG_FILE"
    fi
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 251

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
debug_log() {
    if [[ "${VERBOSE:-false}" == "true" ]]; then
        echo "[DEBUG] $1" >> "$LOG_FILE"
    fi
}

###############################################################################
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 252

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if [[ "${VERBOSE:-false}" == "true" ]]; then
        echo "[DEBUG] $1" >> "$LOG_FILE"
    fi
}

###############################################################################
# Utility Functions
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 253

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        echo "[DEBUG] $1" >> "$LOG_FILE"
    fi
}

###############################################################################
# Utility Functions
###############################################################################
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 254

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi
}

###############################################################################
# Utility Functions
###############################################################################

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 255

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

###############################################################################
# Utility Functions
###############################################################################

print_usage() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 256

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

###############################################################################
# Utility Functions
###############################################################################

print_usage() {
    cat << EOF
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 257

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
###############################################################################
# Utility Functions
###############################################################################

print_usage() {
    cat << EOF
${BLUE}Usage:${NC} $SCRIPT_NAME COMMAND [OPTIONS]
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 258

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Utility Functions
###############################################################################

print_usage() {
    cat << EOF
${BLUE}Usage:${NC} $SCRIPT_NAME COMMAND [OPTIONS]

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 259

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
###############################################################################

print_usage() {
    cat << EOF
${BLUE}Usage:${NC} $SCRIPT_NAME COMMAND [OPTIONS]

${BLUE}Description:${NC}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 294

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
  # Security report
  $SCRIPT_NAME security-report
EOF
}

print_version() {
    echo "$SCRIPT_NAME version $SCRIPT_VERSION"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 295

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
  $SCRIPT_NAME security-report
EOF
}

print_version() {
    echo "$SCRIPT_NAME version $SCRIPT_VERSION"
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 296

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
EOF
}

print_version() {
    echo "$SCRIPT_NAME version $SCRIPT_VERSION"
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 297

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

print_version() {
    echo "$SCRIPT_NAME version $SCRIPT_VERSION"
}

generate_token() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 298

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

print_version() {
    echo "$SCRIPT_NAME version $SCRIPT_VERSION"
}

generate_token() {
    local length=${1:-$CONSOLE_TOKEN_LENGTH}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 299

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
print_version() {
    echo "$SCRIPT_NAME version $SCRIPT_VERSION"
}

generate_token() {
    local length=${1:-$CONSOLE_TOKEN_LENGTH}
    local token
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 300

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "$SCRIPT_NAME version $SCRIPT_VERSION"
}

generate_token() {
    local length=${1:-$CONSOLE_TOKEN_LENGTH}
    local token
    token=$(head -c 32 /dev/urandom | base64 | tr -d '+/=' | cut -c1-"$length")
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 301

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

generate_token() {
    local length=${1:-$CONSOLE_TOKEN_LENGTH}
    local token
    token=$(head -c 32 /dev/urandom | base64 | tr -d '+/=' | cut -c1-"$length")
    echo "$token"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 302

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

generate_token() {
    local length=${1:-$CONSOLE_TOKEN_LENGTH}
    local token
    token=$(head -c 32 /dev/urandom | base64 | tr -d '+/=' | cut -c1-"$length")
    echo "$token"
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 303

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
generate_token() {
    local length=${1:-$CONSOLE_TOKEN_LENGTH}
    local token
    token=$(head -c 32 /dev/urandom | base64 | tr -d '+/=' | cut -c1-"$length")
    echo "$token"
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 304

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local length=${1:-$CONSOLE_TOKEN_LENGTH}
    local token
    token=$(head -c 32 /dev/urandom | base64 | tr -d '+/=' | cut -c1-"$length")
    echo "$token"
}

validate_vm_name() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 305

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local token
    token=$(head -c 32 /dev/urandom | base64 | tr -d '+/=' | cut -c1-"$length")
    echo "$token"
}

validate_vm_name() {
    local vm_name="$1"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 306

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    token=$(head -c 32 /dev/urandom | base64 | tr -d '+/=' | cut -c1-"$length")
    echo "$token"
}

validate_vm_name() {
    local vm_name="$1"
    if ! [[ "$vm_name" =~ ^[a-zA-Z0-9._-]+$ ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 307

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "$token"
}

validate_vm_name() {
    local vm_name="$1"
    if ! [[ "$vm_name" =~ ^[a-zA-Z0-9._-]+$ ]]; then
        log_error "Invalid VM name: $vm_name"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 308

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

validate_vm_name() {
    local vm_name="$1"
    if ! [[ "$vm_name" =~ ^[a-zA-Z0-9._-]+$ ]]; then
        log_error "Invalid VM name: $vm_name"
        return 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 309

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

validate_vm_name() {
    local vm_name="$1"
    if ! [[ "$vm_name" =~ ^[a-zA-Z0-9._-]+$ ]]; then
        log_error "Invalid VM name: $vm_name"
        return 1
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 310

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
validate_vm_name() {
    local vm_name="$1"
    if ! [[ "$vm_name" =~ ^[a-zA-Z0-9._-]+$ ]]; then
        log_error "Invalid VM name: $vm_name"
        return 1
    fi
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 311

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local vm_name="$1"
    if ! [[ "$vm_name" =~ ^[a-zA-Z0-9._-]+$ ]]; then
        log_error "Invalid VM name: $vm_name"
        return 1
    fi
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 312

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if ! [[ "$vm_name" =~ ^[a-zA-Z0-9._-]+$ ]]; then
        log_error "Invalid VM name: $vm_name"
        return 1
    fi
}

validate_port() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 313

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        log_error "Invalid VM name: $vm_name"
        return 1
    fi
}

validate_port() {
    local port="$1"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 314

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        return 1
    fi
}

validate_port() {
    local port="$1"
    if ! [[ "$port" =~ ^[0-9]+$ ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 315

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi
}

validate_port() {
    local port="$1"
    if ! [[ "$port" =~ ^[0-9]+$ ]]; then
        log_error "Invalid port: $port"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 316

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

validate_port() {
    local port="$1"
    if ! [[ "$port" =~ ^[0-9]+$ ]]; then
        log_error "Invalid port: $port"
        return 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 317

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

validate_port() {
    local port="$1"
    if ! [[ "$port" =~ ^[0-9]+$ ]]; then
        log_error "Invalid port: $port"
        return 1
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 318

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
validate_port() {
    local port="$1"
    if ! [[ "$port" =~ ^[0-9]+$ ]]; then
        log_error "Invalid port: $port"
        return 1
    fi
    if ((port < 0 || port > 65535)); then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 319

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local port="$1"
    if ! [[ "$port" =~ ^[0-9]+$ ]]; then
        log_error "Invalid port: $port"
        return 1
    fi
    if ((port < 0 || port > 65535)); then
        log_error "Port out of range: $port"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 320

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if ! [[ "$port" =~ ^[0-9]+$ ]]; then
        log_error "Invalid port: $port"
        return 1
    fi
    if ((port < 0 || port > 65535)); then
        log_error "Port out of range: $port"
        return 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 321

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        log_error "Invalid port: $port"
        return 1
    fi
    if ((port < 0 || port > 65535)); then
        log_error "Port out of range: $port"
        return 1
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 322

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        return 1
    fi
    if ((port < 0 || port > 65535)); then
        log_error "Port out of range: $port"
        return 1
    fi
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 323

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi
    if ((port < 0 || port > 65535)); then
        log_error "Port out of range: $port"
        return 1
    fi
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 324

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if ((port < 0 || port > 65535)); then
        log_error "Port out of range: $port"
        return 1
    fi
}

###############################################################################
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 325

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        log_error "Port out of range: $port"
        return 1
    fi
}

###############################################################################
# VNC Management Functions
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 326

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        return 1
    fi
}

###############################################################################
# VNC Management Functions
###############################################################################
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 327

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi
}

###############################################################################
# VNC Management Functions
###############################################################################

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 328

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

###############################################################################
# VNC Management Functions
###############################################################################

ensure_vnc_port() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 329

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

###############################################################################
# VNC Management Functions
###############################################################################

ensure_vnc_port() {
    local vm_name="$1"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 330

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
###############################################################################
# VNC Management Functions
###############################################################################

ensure_vnc_port() {
    local vm_name="$1"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 331

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# VNC Management Functions
###############################################################################

ensure_vnc_port() {
    local vm_name="$1"

    log_info "Ensuring VNC port for VM: $vm_name"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 332

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
###############################################################################

ensure_vnc_port() {
    local vm_name="$1"

    log_info "Ensuring VNC port for VM: $vm_name"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 333

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

ensure_vnc_port() {
    local vm_name="$1"

    log_info "Ensuring VNC port for VM: $vm_name"

    validate_vm_name "$vm_name" || return 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 334

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
ensure_vnc_port() {
    local vm_name="$1"

    log_info "Ensuring VNC port for VM: $vm_name"

    validate_vm_name "$vm_name" || return 1

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 335

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local vm_name="$1"

    log_info "Ensuring VNC port for VM: $vm_name"

    validate_vm_name "$vm_name" || return 1

    # Check if VM exists (using virsh if available)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 336

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    log_info "Ensuring VNC port for VM: $vm_name"

    validate_vm_name "$vm_name" || return 1

    # Check if VM exists (using virsh if available)
    if command -v virsh &>/dev/null; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 337

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    log_info "Ensuring VNC port for VM: $vm_name"

    validate_vm_name "$vm_name" || return 1

    # Check if VM exists (using virsh if available)
    if command -v virsh &>/dev/null; then
        if ! virsh list --all | grep -q "$vm_name"; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 338

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    validate_vm_name "$vm_name" || return 1

    # Check if VM exists (using virsh if available)
    if command -v virsh &>/dev/null; then
        if ! virsh list --all | grep -q "$vm_name"; then
            log_error "VM not found: $vm_name"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 339

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    validate_vm_name "$vm_name" || return 1

    # Check if VM exists (using virsh if available)
    if command -v virsh &>/dev/null; then
        if ! virsh list --all | grep -q "$vm_name"; then
            log_error "VM not found: $vm_name"
            audit_log_entry "ensure_port" "$vm_name" "failed" "VM not found"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 340

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    # Check if VM exists (using virsh if available)
    if command -v virsh &>/dev/null; then
        if ! virsh list --all | grep -q "$vm_name"; then
            log_error "VM not found: $vm_name"
            audit_log_entry "ensure_port" "$vm_name" "failed" "VM not found"
            return 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 341

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    # Check if VM exists (using virsh if available)
    if command -v virsh &>/dev/null; then
        if ! virsh list --all | grep -q "$vm_name"; then
            log_error "VM not found: $vm_name"
            audit_log_entry "ensure_port" "$vm_name" "failed" "VM not found"
            return 1
        fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 342

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if command -v virsh &>/dev/null; then
        if ! virsh list --all | grep -q "$vm_name"; then
            log_error "VM not found: $vm_name"
            audit_log_entry "ensure_port" "$vm_name" "failed" "VM not found"
            return 1
        fi
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 343

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        if ! virsh list --all | grep -q "$vm_name"; then
            log_error "VM not found: $vm_name"
            audit_log_entry "ensure_port" "$vm_name" "failed" "VM not found"
            return 1
        fi
    fi

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 344

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            log_error "VM not found: $vm_name"
            audit_log_entry "ensure_port" "$vm_name" "failed" "VM not found"
            return 1
        fi
    fi

    # Check if VNC is already configured
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 345

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            audit_log_entry "ensure_port" "$vm_name" "failed" "VM not found"
            return 1
        fi
    fi

    # Check if VNC is already configured
    if virsh dumpxml "$vm_name" 2>/dev/null | grep -q "graphics type='vnc'"; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 346

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            return 1
        fi
    fi

    # Check if VNC is already configured
    if virsh dumpxml "$vm_name" 2>/dev/null | grep -q "graphics type='vnc'"; then
        log_info "VNC already configured for $vm_name"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 347

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        fi
    fi

    # Check if VNC is already configured
    if virsh dumpxml "$vm_name" 2>/dev/null | grep -q "graphics type='vnc'"; then
        log_info "VNC already configured for $vm_name"
    else
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 348

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi

    # Check if VNC is already configured
    if virsh dumpxml "$vm_name" 2>/dev/null | grep -q "graphics type='vnc'"; then
        log_info "VNC already configured for $vm_name"
    else
        log_info "Configuring VNC for $vm_name"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 349

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    # Check if VNC is already configured
    if virsh dumpxml "$vm_name" 2>/dev/null | grep -q "graphics type='vnc'"; then
        log_info "VNC already configured for $vm_name"
    else
        log_info "Configuring VNC for $vm_name"
        if [[ "$DRY_RUN" != "true" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 350

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    # Check if VNC is already configured
    if virsh dumpxml "$vm_name" 2>/dev/null | grep -q "graphics type='vnc'"; then
        log_info "VNC already configured for $vm_name"
    else
        log_info "Configuring VNC for $vm_name"
        if [[ "$DRY_RUN" != "true" ]]; then
            # This would use XML modification in production
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 351

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if virsh dumpxml "$vm_name" 2>/dev/null | grep -q "graphics type='vnc'"; then
        log_info "VNC already configured for $vm_name"
    else
        log_info "Configuring VNC for $vm_name"
        if [[ "$DRY_RUN" != "true" ]]; then
            # This would use XML modification in production
            debug_log "Would configure VNC for VM: $vm_name"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 352

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        log_info "VNC already configured for $vm_name"
    else
        log_info "Configuring VNC for $vm_name"
        if [[ "$DRY_RUN" != "true" ]]; then
            # This would use XML modification in production
            debug_log "Would configure VNC for VM: $vm_name"
        fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 353

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    else
        log_info "Configuring VNC for $vm_name"
        if [[ "$DRY_RUN" != "true" ]]; then
            # This would use XML modification in production
            debug_log "Would configure VNC for VM: $vm_name"
        fi
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 354

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        log_info "Configuring VNC for $vm_name"
        if [[ "$DRY_RUN" != "true" ]]; then
            # This would use XML modification in production
            debug_log "Would configure VNC for VM: $vm_name"
        fi
    fi

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 355

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        if [[ "$DRY_RUN" != "true" ]]; then
            # This would use XML modification in production
            debug_log "Would configure VNC for VM: $vm_name"
        fi
    fi

    # Find available VNC port
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 356

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            # This would use XML modification in production
            debug_log "Would configure VNC for VM: $vm_name"
        fi
    fi

    # Find available VNC port
    local vnc_port
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 357

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            debug_log "Would configure VNC for VM: $vm_name"
        fi
    fi

    # Find available VNC port
    local vnc_port
    vnc_port=$(find_available_vnc_port) || return 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 358

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        fi
    fi

    # Find available VNC port
    local vnc_port
    vnc_port=$(find_available_vnc_port) || return 1

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 359

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi

    # Find available VNC port
    local vnc_port
    vnc_port=$(find_available_vnc_port) || return 1

    log_success "VNC port: $vnc_port for VM: $vm_name"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 360

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    # Find available VNC port
    local vnc_port
    vnc_port=$(find_available_vnc_port) || return 1

    log_success "VNC port: $vnc_port for VM: $vm_name"
    audit_log_entry "ensure_port" "$vm_name" "success" "port=$vnc_port"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 361

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    # Find available VNC port
    local vnc_port
    vnc_port=$(find_available_vnc_port) || return 1

    log_success "VNC port: $vnc_port for VM: $vm_name"
    audit_log_entry "ensure_port" "$vm_name" "success" "port=$vnc_port"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 362

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local vnc_port
    vnc_port=$(find_available_vnc_port) || return 1

    log_success "VNC port: $vnc_port for VM: $vm_name"
    audit_log_entry "ensure_port" "$vm_name" "success" "port=$vnc_port"

    if [[ "${JSON_OUTPUT:-false}" == "true" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 363

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    vnc_port=$(find_available_vnc_port) || return 1

    log_success "VNC port: $vnc_port for VM: $vm_name"
    audit_log_entry "ensure_port" "$vm_name" "success" "port=$vnc_port"

    if [[ "${JSON_OUTPUT:-false}" == "true" ]]; then
        cat << EOF
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 364

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    log_success "VNC port: $vnc_port for VM: $vm_name"
    audit_log_entry "ensure_port" "$vm_name" "success" "port=$vnc_port"

    if [[ "${JSON_OUTPUT:-false}" == "true" ]]; then
        cat << EOF
{
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 365

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    log_success "VNC port: $vnc_port for VM: $vm_name"
    audit_log_entry "ensure_port" "$vm_name" "success" "port=$vnc_port"

    if [[ "${JSON_OUTPUT:-false}" == "true" ]]; then
        cat << EOF
{
  "vm_name": "$vm_name",
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 366

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    audit_log_entry "ensure_port" "$vm_name" "success" "port=$vnc_port"

    if [[ "${JSON_OUTPUT:-false}" == "true" ]]; then
        cat << EOF
{
  "vm_name": "$vm_name",
  "vnc_port": $vnc_port,
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 373

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
  "status": "success"
}
EOF
    fi
}

find_available_vnc_port() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 374

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}
EOF
    fi
}

find_available_vnc_port() {
    for port in $(seq "$MIN_VNC_PORT" "$MAX_VNC_PORT"); do
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 375

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
EOF
    fi
}

find_available_vnc_port() {
    for port in $(seq "$MIN_VNC_PORT" "$MAX_VNC_PORT"); do
        if ! nc -z localhost "$port" 2>/dev/null; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 376

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi
}

find_available_vnc_port() {
    for port in $(seq "$MIN_VNC_PORT" "$MAX_VNC_PORT"); do
        if ! nc -z localhost "$port" 2>/dev/null; then
            echo "$port"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 377

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

find_available_vnc_port() {
    for port in $(seq "$MIN_VNC_PORT" "$MAX_VNC_PORT"); do
        if ! nc -z localhost "$port" 2>/dev/null; then
            echo "$port"
            return 0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 378

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

find_available_vnc_port() {
    for port in $(seq "$MIN_VNC_PORT" "$MAX_VNC_PORT"); do
        if ! nc -z localhost "$port" 2>/dev/null; then
            echo "$port"
            return 0
        fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 379

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
find_available_vnc_port() {
    for port in $(seq "$MIN_VNC_PORT" "$MAX_VNC_PORT"); do
        if ! nc -z localhost "$port" 2>/dev/null; then
            echo "$port"
            return 0
        fi
    done
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 380

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    for port in $(seq "$MIN_VNC_PORT" "$MAX_VNC_PORT"); do
        if ! nc -z localhost "$port" 2>/dev/null; then
            echo "$port"
            return 0
        fi
    done
    log_error "No available VNC ports"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 381

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        if ! nc -z localhost "$port" 2>/dev/null; then
            echo "$port"
            return 0
        fi
    done
    log_error "No available VNC ports"
    return 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 382

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            echo "$port"
            return 0
        fi
    done
    log_error "No available VNC ports"
    return 1
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 383

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            return 0
        fi
    done
    log_error "No available VNC ports"
    return 1
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 384

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        fi
    done
    log_error "No available VNC ports"
    return 1
}

validate_vnc_reachable() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 385

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    done
    log_error "No available VNC ports"
    return 1
}

validate_vnc_reachable() {
    local host_port="$1"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 386

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    log_error "No available VNC ports"
    return 1
}

validate_vnc_reachable() {
    local host_port="$1"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 387

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    return 1
}

validate_vnc_reachable() {
    local host_port="$1"

    log_info "Validating VNC at: $host_port"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 388

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

validate_vnc_reachable() {
    local host_port="$1"

    log_info "Validating VNC at: $host_port"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 389

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

validate_vnc_reachable() {
    local host_port="$1"

    log_info "Validating VNC at: $host_port"

    if [[ ! "$host_port" =~ ^[^:]+:[0-9]+$ ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 390

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
validate_vnc_reachable() {
    local host_port="$1"

    log_info "Validating VNC at: $host_port"

    if [[ ! "$host_port" =~ ^[^:]+:[0-9]+$ ]]; then
        log_error "Invalid host:port format: $host_port"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 391

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local host_port="$1"

    log_info "Validating VNC at: $host_port"

    if [[ ! "$host_port" =~ ^[^:]+:[0-9]+$ ]]; then
        log_error "Invalid host:port format: $host_port"
        return 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 392

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    log_info "Validating VNC at: $host_port"

    if [[ ! "$host_port" =~ ^[^:]+:[0-9]+$ ]]; then
        log_error "Invalid host:port format: $host_port"
        return 1
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 393

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    log_info "Validating VNC at: $host_port"

    if [[ ! "$host_port" =~ ^[^:]+:[0-9]+$ ]]; then
        log_error "Invalid host:port format: $host_port"
        return 1
    fi

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 394

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    if [[ ! "$host_port" =~ ^[^:]+:[0-9]+$ ]]; then
        log_error "Invalid host:port format: $host_port"
        return 1
    fi

    local host="${host_port%:*}"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 395

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if [[ ! "$host_port" =~ ^[^:]+:[0-9]+$ ]]; then
        log_error "Invalid host:port format: $host_port"
        return 1
    fi

    local host="${host_port%:*}"
    local port="${host_port##*:}"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 396

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        log_error "Invalid host:port format: $host_port"
        return 1
    fi

    local host="${host_port%:*}"
    local port="${host_port##*:}"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 397

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        return 1
    fi

    local host="${host_port%:*}"
    local port="${host_port##*:}"

    validate_port "$port" || return 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 398

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi

    local host="${host_port%:*}"
    local port="${host_port##*:}"

    validate_port "$port" || return 1

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 399

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    local host="${host_port%:*}"
    local port="${host_port##*:}"

    validate_port "$port" || return 1

    # Check if port is within allowed VNC range
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 400

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local host="${host_port%:*}"
    local port="${host_port##*:}"

    validate_port "$port" || return 1

    # Check if port is within allowed VNC range
    if ! [[ "$port" =~ ^($(IFS=\|; echo "${ALLOWED_PORTS[*]}"))$ ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 401

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local port="${host_port##*:}"

    validate_port "$port" || return 1

    # Check if port is within allowed VNC range
    if ! [[ "$port" =~ ^($(IFS=\|; echo "${ALLOWED_PORTS[*]}"))$ ]]; then
        log_warn "Port $port outside standard VNC range"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 402

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    validate_port "$port" || return 1

    # Check if port is within allowed VNC range
    if ! [[ "$port" =~ ^($(IFS=\|; echo "${ALLOWED_PORTS[*]}"))$ ]]; then
        log_warn "Port $port outside standard VNC range"
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 403

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    validate_port "$port" || return 1

    # Check if port is within allowed VNC range
    if ! [[ "$port" =~ ^($(IFS=\|; echo "${ALLOWED_PORTS[*]}"))$ ]]; then
        log_warn "Port $port outside standard VNC range"
    fi

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 404

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    # Check if port is within allowed VNC range
    if ! [[ "$port" =~ ^($(IFS=\|; echo "${ALLOWED_PORTS[*]}"))$ ]]; then
        log_warn "Port $port outside standard VNC range"
    fi

    # Test connectivity
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 405

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    # Check if port is within allowed VNC range
    if ! [[ "$port" =~ ^($(IFS=\|; echo "${ALLOWED_PORTS[*]}"))$ ]]; then
        log_warn "Port $port outside standard VNC range"
    fi

    # Test connectivity
    if timeout 5 nc -zv "$host" "$port" 2>&1 | grep -q "succeeded"; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 406

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if ! [[ "$port" =~ ^($(IFS=\|; echo "${ALLOWED_PORTS[*]}"))$ ]]; then
        log_warn "Port $port outside standard VNC range"
    fi

    # Test connectivity
    if timeout 5 nc -zv "$host" "$port" 2>&1 | grep -q "succeeded"; then
        log_success "VNC is reachable: $host_port"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 407

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        log_warn "Port $port outside standard VNC range"
    fi

    # Test connectivity
    if timeout 5 nc -zv "$host" "$port" 2>&1 | grep -q "succeeded"; then
        log_success "VNC is reachable: $host_port"
        audit_log_entry "validate_vnc" "$host_port" "success" "reachable"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 408

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi

    # Test connectivity
    if timeout 5 nc -zv "$host" "$port" 2>&1 | grep -q "succeeded"; then
        log_success "VNC is reachable: $host_port"
        audit_log_entry "validate_vnc" "$host_port" "success" "reachable"
        return 0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 409

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    # Test connectivity
    if timeout 5 nc -zv "$host" "$port" 2>&1 | grep -q "succeeded"; then
        log_success "VNC is reachable: $host_port"
        audit_log_entry "validate_vnc" "$host_port" "success" "reachable"
        return 0
    else
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 410

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    # Test connectivity
    if timeout 5 nc -zv "$host" "$port" 2>&1 | grep -q "succeeded"; then
        log_success "VNC is reachable: $host_port"
        audit_log_entry "validate_vnc" "$host_port" "success" "reachable"
        return 0
    else
        log_error "VNC is not reachable: $host_port"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 411

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if timeout 5 nc -zv "$host" "$port" 2>&1 | grep -q "succeeded"; then
        log_success "VNC is reachable: $host_port"
        audit_log_entry "validate_vnc" "$host_port" "success" "reachable"
        return 0
    else
        log_error "VNC is not reachable: $host_port"
        audit_log_entry "validate_vnc" "$host_port" "failed" "unreachable"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 412

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        log_success "VNC is reachable: $host_port"
        audit_log_entry "validate_vnc" "$host_port" "success" "reachable"
        return 0
    else
        log_error "VNC is not reachable: $host_port"
        audit_log_entry "validate_vnc" "$host_port" "failed" "unreachable"
        return 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 413

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        audit_log_entry "validate_vnc" "$host_port" "success" "reachable"
        return 0
    else
        log_error "VNC is not reachable: $host_port"
        audit_log_entry "validate_vnc" "$host_port" "failed" "unreachable"
        return 1
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 414

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        return 0
    else
        log_error "VNC is not reachable: $host_port"
        audit_log_entry "validate_vnc" "$host_port" "failed" "unreachable"
        return 1
    fi
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 415

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    else
        log_error "VNC is not reachable: $host_port"
        audit_log_entry "validate_vnc" "$host_port" "failed" "unreachable"
        return 1
    fi
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 416

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        log_error "VNC is not reachable: $host_port"
        audit_log_entry "validate_vnc" "$host_port" "failed" "unreachable"
        return 1
    fi
}

###############################################################################
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 417

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        audit_log_entry "validate_vnc" "$host_port" "failed" "unreachable"
        return 1
    fi
}

###############################################################################
# Console Token Management
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 418

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        return 1
    fi
}

###############################################################################
# Console Token Management
###############################################################################
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 419

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi
}

###############################################################################
# Console Token Management
###############################################################################

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 420

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

###############################################################################
# Console Token Management
###############################################################################

generate_console_token() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 421

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

###############################################################################
# Console Token Management
###############################################################################

generate_console_token() {
    local vm_name="$1"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 422

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
###############################################################################
# Console Token Management
###############################################################################

generate_console_token() {
    local vm_name="$1"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 423

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Console Token Management
###############################################################################

generate_console_token() {
    local vm_name="$1"

    log_info "Generating console token for VM: $vm_name"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 424

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
###############################################################################

generate_console_token() {
    local vm_name="$1"

    log_info "Generating console token for VM: $vm_name"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 425

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

generate_console_token() {
    local vm_name="$1"

    log_info "Generating console token for VM: $vm_name"

    validate_vm_name "$vm_name" || return 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 426

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
generate_console_token() {
    local vm_name="$1"

    log_info "Generating console token for VM: $vm_name"

    validate_vm_name "$vm_name" || return 1

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 427

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local vm_name="$1"

    log_info "Generating console token for VM: $vm_name"

    validate_vm_name "$vm_name" || return 1

    local token
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 428

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    log_info "Generating console token for VM: $vm_name"

    validate_vm_name "$vm_name" || return 1

    local token
    token=$(generate_token)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 429

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    log_info "Generating console token for VM: $vm_name"

    validate_vm_name "$vm_name" || return 1

    local token
    token=$(generate_token)

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 430

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    validate_vm_name "$vm_name" || return 1

    local token
    token=$(generate_token)

    local expiration=$(($(date +%s) + CONSOLE_TOKEN_TTL))
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 431

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    validate_vm_name "$vm_name" || return 1

    local token
    token=$(generate_token)

    local expiration=$(($(date +%s) + CONSOLE_TOKEN_TTL))
    local token_type="${2:-read-write}"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 432

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    local token
    token=$(generate_token)

    local expiration=$(($(date +%s) + CONSOLE_TOKEN_TTL))
    local token_type="${2:-read-write}"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 433

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local token
    token=$(generate_token)

    local expiration=$(($(date +%s) + CONSOLE_TOKEN_TTL))
    local token_type="${2:-read-write}"

    # Validate token type
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 434

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    token=$(generate_token)

    local expiration=$(($(date +%s) + CONSOLE_TOKEN_TTL))
    local token_type="${2:-read-write}"

    # Validate token type
    if ! [[ "$token_type" =~ ^(read-only|read-write|admin)$ ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 435

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    local expiration=$(($(date +%s) + CONSOLE_TOKEN_TTL))
    local token_type="${2:-read-write}"

    # Validate token type
    if ! [[ "$token_type" =~ ^(read-only|read-write|admin)$ ]]; then
        log_error "Invalid token type: $token_type"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 436

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local expiration=$(($(date +%s) + CONSOLE_TOKEN_TTL))
    local token_type="${2:-read-write}"

    # Validate token type
    if ! [[ "$token_type" =~ ^(read-only|read-write|admin)$ ]]; then
        log_error "Invalid token type: $token_type"
        return 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 437

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local token_type="${2:-read-write}"

    # Validate token type
    if ! [[ "$token_type" =~ ^(read-only|read-write|admin)$ ]]; then
        log_error "Invalid token type: $token_type"
        return 1
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 438

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    # Validate token type
    if ! [[ "$token_type" =~ ^(read-only|read-write|admin)$ ]]; then
        log_error "Invalid token type: $token_type"
        return 1
    fi

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 439

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    # Validate token type
    if ! [[ "$token_type" =~ ^(read-only|read-write|admin)$ ]]; then
        log_error "Invalid token type: $token_type"
        return 1
    fi

    if [[ "$DRY_RUN" != "true" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 440

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if ! [[ "$token_type" =~ ^(read-only|read-write|admin)$ ]]; then
        log_error "Invalid token type: $token_type"
        return 1
    fi

    if [[ "$DRY_RUN" != "true" ]]; then
        # Store token with metadata
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 441

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        log_error "Invalid token type: $token_type"
        return 1
    fi

    if [[ "$DRY_RUN" != "true" ]]; then
        # Store token with metadata
        mkdir -p "$STATE_DIR"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 442

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        return 1
    fi

    if [[ "$DRY_RUN" != "true" ]]; then
        # Store token with metadata
        mkdir -p "$STATE_DIR"
        local token_file="$STATE_DIR/${token}.token"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 443

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi

    if [[ "$DRY_RUN" != "true" ]]; then
        # Store token with metadata
        mkdir -p "$STATE_DIR"
        local token_file="$STATE_DIR/${token}.token"
        cat > "$token_file" << EOF
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 444

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    if [[ "$DRY_RUN" != "true" ]]; then
        # Store token with metadata
        mkdir -p "$STATE_DIR"
        local token_file="$STATE_DIR/${token}.token"
        cat > "$token_file" << EOF
{
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 445

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if [[ "$DRY_RUN" != "true" ]]; then
        # Store token with metadata
        mkdir -p "$STATE_DIR"
        local token_file="$STATE_DIR/${token}.token"
        cat > "$token_file" << EOF
{
  "vm_name": "$vm_name",
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 446

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        # Store token with metadata
        mkdir -p "$STATE_DIR"
        local token_file="$STATE_DIR/${token}.token"
        cat > "$token_file" << EOF
{
  "vm_name": "$vm_name",
  "token": "$token",
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 457

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
  "ip": "${SSH_CLIENT%% *}"
}
EOF
        chmod 600 "$token_file"
        audit_log_entry "generate_token" "$vm_name" "success" "type=$token_type"
    fi

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 458

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}
EOF
        chmod 600 "$token_file"
        audit_log_entry "generate_token" "$vm_name" "success" "type=$token_type"
    fi

    log_success "Console token generated for $vm_name"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 459

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
EOF
        chmod 600 "$token_file"
        audit_log_entry "generate_token" "$vm_name" "success" "type=$token_type"
    fi

    log_success "Console token generated for $vm_name"
    echo ""
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 460

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        chmod 600 "$token_file"
        audit_log_entry "generate_token" "$vm_name" "success" "type=$token_type"
    fi

    log_success "Console token generated for $vm_name"
    echo ""
    echo "Token: $token"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 461

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        audit_log_entry "generate_token" "$vm_name" "success" "type=$token_type"
    fi

    log_success "Console token generated for $vm_name"
    echo ""
    echo "Token: $token"
    echo "Type: $token_type"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 462

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi

    log_success "Console token generated for $vm_name"
    echo ""
    echo "Token: $token"
    echo "Type: $token_type"
    echo "TTL: ${CONSOLE_TOKEN_TTL}s"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 463

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    log_success "Console token generated for $vm_name"
    echo ""
    echo "Token: $token"
    echo "Type: $token_type"
    echo "TTL: ${CONSOLE_TOKEN_TTL}s"
    echo "Expires: $(date -d @$expiration)"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 464

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    log_success "Console token generated for $vm_name"
    echo ""
    echo "Token: $token"
    echo "Type: $token_type"
    echo "TTL: ${CONSOLE_TOKEN_TTL}s"
    echo "Expires: $(date -d @$expiration)"
    echo ""
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 465

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo ""
    echo "Token: $token"
    echo "Type: $token_type"
    echo "TTL: ${CONSOLE_TOKEN_TTL}s"
    echo "Expires: $(date -d @$expiration)"
    echo ""
    echo "Usage:"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 466

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "Token: $token"
    echo "Type: $token_type"
    echo "TTL: ${CONSOLE_TOKEN_TTL}s"
    echo "Expires: $(date -d @$expiration)"
    echo ""
    echo "Usage:"
    echo "  URL: vnc://$(hostname):5900?token=$token"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 467

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "Type: $token_type"
    echo "TTL: ${CONSOLE_TOKEN_TTL}s"
    echo "Expires: $(date -d @$expiration)"
    echo ""
    echo "Usage:"
    echo "  URL: vnc://$(hostname):5900?token=$token"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 468

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "TTL: ${CONSOLE_TOKEN_TTL}s"
    echo "Expires: $(date -d @$expiration)"
    echo ""
    echo "Usage:"
    echo "  URL: vnc://$(hostname):5900?token=$token"

    if [[ "${JSON_OUTPUT:-false}" == "true" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 469

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "Expires: $(date -d @$expiration)"
    echo ""
    echo "Usage:"
    echo "  URL: vnc://$(hostname):5900?token=$token"

    if [[ "${JSON_OUTPUT:-false}" == "true" ]]; then
        cat << EOF
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 470

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo ""
    echo "Usage:"
    echo "  URL: vnc://$(hostname):5900?token=$token"

    if [[ "${JSON_OUTPUT:-false}" == "true" ]]; then
        cat << EOF
{
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 471

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "Usage:"
    echo "  URL: vnc://$(hostname):5900?token=$token"

    if [[ "${JSON_OUTPUT:-false}" == "true" ]]; then
        cat << EOF
{
  "token": "$token",
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 472

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "  URL: vnc://$(hostname):5900?token=$token"

    if [[ "${JSON_OUTPUT:-false}" == "true" ]]; then
        cat << EOF
{
  "token": "$token",
  "vm_name": "$vm_name",
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 481

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
  "expires_at": "$(date -d @$expiration -Iseconds)"
}
EOF
    fi
}

validate_console_token() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 482

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}
EOF
    fi
}

validate_console_token() {
    local token="$1"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 483

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
EOF
    fi
}

validate_console_token() {
    local token="$1"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 484

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi
}

validate_console_token() {
    local token="$1"

    local token_file="$STATE_DIR/${token}.token"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 485

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

validate_console_token() {
    local token="$1"

    local token_file="$STATE_DIR/${token}.token"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 486

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

validate_console_token() {
    local token="$1"

    local token_file="$STATE_DIR/${token}.token"

    if [[ ! -f "$token_file" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 487

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
validate_console_token() {
    local token="$1"

    local token_file="$STATE_DIR/${token}.token"

    if [[ ! -f "$token_file" ]]; then
        log_error "Token not found: $token"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 488

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local token="$1"

    local token_file="$STATE_DIR/${token}.token"

    if [[ ! -f "$token_file" ]]; then
        log_error "Token not found: $token"
        return 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 489

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    local token_file="$STATE_DIR/${token}.token"

    if [[ ! -f "$token_file" ]]; then
        log_error "Token not found: $token"
        return 1
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 490

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local token_file="$STATE_DIR/${token}.token"

    if [[ ! -f "$token_file" ]]; then
        log_error "Token not found: $token"
        return 1
    fi

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 491

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    if [[ ! -f "$token_file" ]]; then
        log_error "Token not found: $token"
        return 1
    fi

    # Check if token is expired
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 492

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if [[ ! -f "$token_file" ]]; then
        log_error "Token not found: $token"
        return 1
    fi

    # Check if token is expired
    local expires_at=$(grep "expires_at" "$token_file" | cut -d'"' -f4)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 493

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        log_error "Token not found: $token"
        return 1
    fi

    # Check if token is expired
    local expires_at=$(grep "expires_at" "$token_file" | cut -d'"' -f4)
    local current_time=$(date -Iseconds)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 494

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        return 1
    fi

    # Check if token is expired
    local expires_at=$(grep "expires_at" "$token_file" | cut -d'"' -f4)
    local current_time=$(date -Iseconds)

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 495

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi

    # Check if token is expired
    local expires_at=$(grep "expires_at" "$token_file" | cut -d'"' -f4)
    local current_time=$(date -Iseconds)

    if [[ "$current_time" > "$expires_at" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 496

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    # Check if token is expired
    local expires_at=$(grep "expires_at" "$token_file" | cut -d'"' -f4)
    local current_time=$(date -Iseconds)

    if [[ "$current_time" > "$expires_at" ]]; then
        log_error "Token expired: $token"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 497

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    # Check if token is expired
    local expires_at=$(grep "expires_at" "$token_file" | cut -d'"' -f4)
    local current_time=$(date -Iseconds)

    if [[ "$current_time" > "$expires_at" ]]; then
        log_error "Token expired: $token"
        rm -f "$token_file"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 498

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local expires_at=$(grep "expires_at" "$token_file" | cut -d'"' -f4)
    local current_time=$(date -Iseconds)

    if [[ "$current_time" > "$expires_at" ]]; then
        log_error "Token expired: $token"
        rm -f "$token_file"
        return 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 499

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local current_time=$(date -Iseconds)

    if [[ "$current_time" > "$expires_at" ]]; then
        log_error "Token expired: $token"
        rm -f "$token_file"
        return 1
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 500

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    if [[ "$current_time" > "$expires_at" ]]; then
        log_error "Token expired: $token"
        rm -f "$token_file"
        return 1
    fi

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 501

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if [[ "$current_time" > "$expires_at" ]]; then
        log_error "Token expired: $token"
        rm -f "$token_file"
        return 1
    fi

    log_success "Token is valid: $token"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 502

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        log_error "Token expired: $token"
        rm -f "$token_file"
        return 1
    fi

    log_success "Token is valid: $token"
    return 0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 503

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        rm -f "$token_file"
        return 1
    fi

    log_success "Token is valid: $token"
    return 0
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 504

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        return 1
    fi

    log_success "Token is valid: $token"
    return 0
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 505

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi

    log_success "Token is valid: $token"
    return 0
}

###############################################################################
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 506

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    log_success "Token is valid: $token"
    return 0
}

###############################################################################
# Session Management
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 507

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    log_success "Token is valid: $token"
    return 0
}

###############################################################################
# Session Management
###############################################################################
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 508

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    return 0
}

###############################################################################
# Session Management
###############################################################################

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 509

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

###############################################################################
# Session Management
###############################################################################

list_console_sessions() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 510

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

###############################################################################
# Session Management
###############################################################################

list_console_sessions() {
    log_info "Active console sessions:"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 511

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
###############################################################################
# Session Management
###############################################################################

list_console_sessions() {
    log_info "Active console sessions:"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 512

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Session Management
###############################################################################

list_console_sessions() {
    log_info "Active console sessions:"

    if [[ ! -d "$STATE_DIR" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 513

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
###############################################################################

list_console_sessions() {
    log_info "Active console sessions:"

    if [[ ! -d "$STATE_DIR" ]]; then
        log_info "No active sessions"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 514

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

list_console_sessions() {
    log_info "Active console sessions:"

    if [[ ! -d "$STATE_DIR" ]]; then
        log_info "No active sessions"
        return 0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 515

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
list_console_sessions() {
    log_info "Active console sessions:"

    if [[ ! -d "$STATE_DIR" ]]; then
        log_info "No active sessions"
        return 0
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 516

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    log_info "Active console sessions:"

    if [[ ! -d "$STATE_DIR" ]]; then
        log_info "No active sessions"
        return 0
    fi

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 517

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    if [[ ! -d "$STATE_DIR" ]]; then
        log_info "No active sessions"
        return 0
    fi

    local count=0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 518

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if [[ ! -d "$STATE_DIR" ]]; then
        log_info "No active sessions"
        return 0
    fi

    local count=0

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 519

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        log_info "No active sessions"
        return 0
    fi

    local count=0

    echo ""
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 520

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        return 0
    fi

    local count=0

    echo ""
    echo "Token | VM Name | Type | Created | Expires"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 521

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi

    local count=0

    echo ""
    echo "Token | VM Name | Type | Created | Expires"
    echo "------|---------|------|---------|--------"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 522

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    local count=0

    echo ""
    echo "Token | VM Name | Type | Created | Expires"
    echo "------|---------|------|---------|--------"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 523

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local count=0

    echo ""
    echo "Token | VM Name | Type | Created | Expires"
    echo "------|---------|------|---------|--------"

    for token_file in "$STATE_DIR"/*.token; do
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 524

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    echo ""
    echo "Token | VM Name | Type | Created | Expires"
    echo "------|---------|------|---------|--------"

    for token_file in "$STATE_DIR"/*.token; do
        if [[ -f "$token_file" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 525

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo ""
    echo "Token | VM Name | Type | Created | Expires"
    echo "------|---------|------|---------|--------"

    for token_file in "$STATE_DIR"/*.token; do
        if [[ -f "$token_file" ]]; then
            ((count++))
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 526

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "Token | VM Name | Type | Created | Expires"
    echo "------|---------|------|---------|--------"

    for token_file in "$STATE_DIR"/*.token; do
        if [[ -f "$token_file" ]]; then
            ((count++))
            local vm_name=$(grep "vm_name" "$token_file" | cut -d'"' -f4)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 527

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "------|---------|------|---------|--------"

    for token_file in "$STATE_DIR"/*.token; do
        if [[ -f "$token_file" ]]; then
            ((count++))
            local vm_name=$(grep "vm_name" "$token_file" | cut -d'"' -f4)
            local token_type=$(grep '"type"' "$token_file" | cut -d'"' -f4)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 528

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    for token_file in "$STATE_DIR"/*.token; do
        if [[ -f "$token_file" ]]; then
            ((count++))
            local vm_name=$(grep "vm_name" "$token_file" | cut -d'"' -f4)
            local token_type=$(grep '"type"' "$token_file" | cut -d'"' -f4)
            local created_at=$(grep "created_at" "$token_file" | cut -d'"' -f4)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 529

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    for token_file in "$STATE_DIR"/*.token; do
        if [[ -f "$token_file" ]]; then
            ((count++))
            local vm_name=$(grep "vm_name" "$token_file" | cut -d'"' -f4)
            local token_type=$(grep '"type"' "$token_file" | cut -d'"' -f4)
            local created_at=$(grep "created_at" "$token_file" | cut -d'"' -f4)
            local expires_at=$(grep "expires_at" "$token_file" | cut -d'"' -f4)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 530

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        if [[ -f "$token_file" ]]; then
            ((count++))
            local vm_name=$(grep "vm_name" "$token_file" | cut -d'"' -f4)
            local token_type=$(grep '"type"' "$token_file" | cut -d'"' -f4)
            local created_at=$(grep "created_at" "$token_file" | cut -d'"' -f4)
            local expires_at=$(grep "expires_at" "$token_file" | cut -d'"' -f4)
            local token_short=$(basename "$token_file" .token | cut -c1-8)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 531

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            ((count++))
            local vm_name=$(grep "vm_name" "$token_file" | cut -d'"' -f4)
            local token_type=$(grep '"type"' "$token_file" | cut -d'"' -f4)
            local created_at=$(grep "created_at" "$token_file" | cut -d'"' -f4)
            local expires_at=$(grep "expires_at" "$token_file" | cut -d'"' -f4)
            local token_short=$(basename "$token_file" .token | cut -c1-8)

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 532

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            local vm_name=$(grep "vm_name" "$token_file" | cut -d'"' -f4)
            local token_type=$(grep '"type"' "$token_file" | cut -d'"' -f4)
            local created_at=$(grep "created_at" "$token_file" | cut -d'"' -f4)
            local expires_at=$(grep "expires_at" "$token_file" | cut -d'"' -f4)
            local token_short=$(basename "$token_file" .token | cut -c1-8)

            echo "$token_short... | $vm_name | $token_type | $created_at | $expires_at"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 533

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            local token_type=$(grep '"type"' "$token_file" | cut -d'"' -f4)
            local created_at=$(grep "created_at" "$token_file" | cut -d'"' -f4)
            local expires_at=$(grep "expires_at" "$token_file" | cut -d'"' -f4)
            local token_short=$(basename "$token_file" .token | cut -c1-8)

            echo "$token_short... | $vm_name | $token_type | $created_at | $expires_at"
        fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 534

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            local created_at=$(grep "created_at" "$token_file" | cut -d'"' -f4)
            local expires_at=$(grep "expires_at" "$token_file" | cut -d'"' -f4)
            local token_short=$(basename "$token_file" .token | cut -c1-8)

            echo "$token_short... | $vm_name | $token_type | $created_at | $expires_at"
        fi
    done
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 535

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            local expires_at=$(grep "expires_at" "$token_file" | cut -d'"' -f4)
            local token_short=$(basename "$token_file" .token | cut -c1-8)

            echo "$token_short... | $vm_name | $token_type | $created_at | $expires_at"
        fi
    done

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 536

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            local token_short=$(basename "$token_file" .token | cut -c1-8)

            echo "$token_short... | $vm_name | $token_type | $created_at | $expires_at"
        fi
    done

    if ((count == 0)); then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 537

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

            echo "$token_short... | $vm_name | $token_type | $created_at | $expires_at"
        fi
    done

    if ((count == 0)); then
        log_info "No active sessions"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 538

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            echo "$token_short... | $vm_name | $token_type | $created_at | $expires_at"
        fi
    done

    if ((count == 0)); then
        log_info "No active sessions"
    else
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 539

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        fi
    done

    if ((count == 0)); then
        log_info "No active sessions"
    else
        log_info "Total sessions: $count"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 540

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    done

    if ((count == 0)); then
        log_info "No active sessions"
    else
        log_info "Total sessions: $count"
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 541

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    if ((count == 0)); then
        log_info "No active sessions"
    else
        log_info "Total sessions: $count"
    fi

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 542

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if ((count == 0)); then
        log_info "No active sessions"
    else
        log_info "Total sessions: $count"
    fi

    if [[ "${JSON_OUTPUT:-false}" == "true" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 543

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        log_info "No active sessions"
    else
        log_info "Total sessions: $count"
    fi

    if [[ "${JSON_OUTPUT:-false}" == "true" ]]; then
        echo "["
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 544

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    else
        log_info "Total sessions: $count"
    fi

    if [[ "${JSON_OUTPUT:-false}" == "true" ]]; then
        echo "["
        local first=true
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 545

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        log_info "Total sessions: $count"
    fi

    if [[ "${JSON_OUTPUT:-false}" == "true" ]]; then
        echo "["
        local first=true
        for token_file in "$STATE_DIR"/*.token; do
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 546

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi

    if [[ "${JSON_OUTPUT:-false}" == "true" ]]; then
        echo "["
        local first=true
        for token_file in "$STATE_DIR"/*.token; do
            if [[ -f "$token_file" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 547

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    if [[ "${JSON_OUTPUT:-false}" == "true" ]]; then
        echo "["
        local first=true
        for token_file in "$STATE_DIR"/*.token; do
            if [[ -f "$token_file" ]]; then
                if [[ "$first" == "false" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 548

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if [[ "${JSON_OUTPUT:-false}" == "true" ]]; then
        echo "["
        local first=true
        for token_file in "$STATE_DIR"/*.token; do
            if [[ -f "$token_file" ]]; then
                if [[ "$first" == "false" ]]; then
                    echo ","
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 549

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        echo "["
        local first=true
        for token_file in "$STATE_DIR"/*.token; do
            if [[ -f "$token_file" ]]; then
                if [[ "$first" == "false" ]]; then
                    echo ","
                fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 550

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        local first=true
        for token_file in "$STATE_DIR"/*.token; do
            if [[ -f "$token_file" ]]; then
                if [[ "$first" == "false" ]]; then
                    echo ","
                fi
                cat "$token_file"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 551

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        for token_file in "$STATE_DIR"/*.token; do
            if [[ -f "$token_file" ]]; then
                if [[ "$first" == "false" ]]; then
                    echo ","
                fi
                cat "$token_file"
                first=false
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 552

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            if [[ -f "$token_file" ]]; then
                if [[ "$first" == "false" ]]; then
                    echo ","
                fi
                cat "$token_file"
                first=false
            fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 553

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
                if [[ "$first" == "false" ]]; then
                    echo ","
                fi
                cat "$token_file"
                first=false
            fi
        done
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 554

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
                    echo ","
                fi
                cat "$token_file"
                first=false
            fi
        done
        echo "]"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 555

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
                fi
                cat "$token_file"
                first=false
            fi
        done
        echo "]"
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 556

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
                cat "$token_file"
                first=false
            fi
        done
        echo "]"
    fi
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 557

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
                first=false
            fi
        done
        echo "]"
    fi
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 558

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            fi
        done
        echo "]"
    fi
}

close_console_session() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 559

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        done
        echo "]"
    fi
}

close_console_session() {
    local token="$1"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 560

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        echo "]"
    fi
}

close_console_session() {
    local token="$1"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 561

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi
}

close_console_session() {
    local token="$1"

    log_info "Closing console session: $token"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 562

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

close_console_session() {
    local token="$1"

    log_info "Closing console session: $token"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 563

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

close_console_session() {
    local token="$1"

    log_info "Closing console session: $token"

    local token_file="$STATE_DIR/${token}.token"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 564

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
close_console_session() {
    local token="$1"

    log_info "Closing console session: $token"

    local token_file="$STATE_DIR/${token}.token"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 565

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local token="$1"

    log_info "Closing console session: $token"

    local token_file="$STATE_DIR/${token}.token"

    if [[ ! -f "$token_file" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 566

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    log_info "Closing console session: $token"

    local token_file="$STATE_DIR/${token}.token"

    if [[ ! -f "$token_file" ]]; then
        log_error "Session not found: $token"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 567

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    log_info "Closing console session: $token"

    local token_file="$STATE_DIR/${token}.token"

    if [[ ! -f "$token_file" ]]; then
        log_error "Session not found: $token"
        return 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 568

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    local token_file="$STATE_DIR/${token}.token"

    if [[ ! -f "$token_file" ]]; then
        log_error "Session not found: $token"
        return 1
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 569

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local token_file="$STATE_DIR/${token}.token"

    if [[ ! -f "$token_file" ]]; then
        log_error "Session not found: $token"
        return 1
    fi

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 570

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    if [[ ! -f "$token_file" ]]; then
        log_error "Session not found: $token"
        return 1
    fi

    if [[ "$DRY_RUN" != "true" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 571

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if [[ ! -f "$token_file" ]]; then
        log_error "Session not found: $token"
        return 1
    fi

    if [[ "$DRY_RUN" != "true" ]]; then
        rm -f "$token_file"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 572

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        log_error "Session not found: $token"
        return 1
    fi

    if [[ "$DRY_RUN" != "true" ]]; then
        rm -f "$token_file"
        log_success "Session closed: $token"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 573

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        return 1
    fi

    if [[ "$DRY_RUN" != "true" ]]; then
        rm -f "$token_file"
        log_success "Session closed: $token"
        audit_log_entry "close_session" "$token" "success"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 574

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi

    if [[ "$DRY_RUN" != "true" ]]; then
        rm -f "$token_file"
        log_success "Session closed: $token"
        audit_log_entry "close_session" "$token" "success"
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 575

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    if [[ "$DRY_RUN" != "true" ]]; then
        rm -f "$token_file"
        log_success "Session closed: $token"
        audit_log_entry "close_session" "$token" "success"
    fi
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 576

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if [[ "$DRY_RUN" != "true" ]]; then
        rm -f "$token_file"
        log_success "Session closed: $token"
        audit_log_entry "close_session" "$token" "success"
    fi
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 577

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        rm -f "$token_file"
        log_success "Session closed: $token"
        audit_log_entry "close_session" "$token" "success"
    fi
}

###############################################################################
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 578

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        log_success "Session closed: $token"
        audit_log_entry "close_session" "$token" "success"
    fi
}

###############################################################################
# Security Functions
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 579

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        audit_log_entry "close_session" "$token" "success"
    fi
}

###############################################################################
# Security Functions
###############################################################################
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 580

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi
}

###############################################################################
# Security Functions
###############################################################################

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 581

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

###############################################################################
# Security Functions
###############################################################################

configure_vnc_security() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 582

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

###############################################################################
# Security Functions
###############################################################################

configure_vnc_security() {
    local vm_name="$1"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 583

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
###############################################################################
# Security Functions
###############################################################################

configure_vnc_security() {
    local vm_name="$1"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 584

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Security Functions
###############################################################################

configure_vnc_security() {
    local vm_name="$1"

    log_info "Configuring VNC security for VM: $vm_name"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 585

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
###############################################################################

configure_vnc_security() {
    local vm_name="$1"

    log_info "Configuring VNC security for VM: $vm_name"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 586

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

configure_vnc_security() {
    local vm_name="$1"

    log_info "Configuring VNC security for VM: $vm_name"

    validate_vm_name "$vm_name" || return 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 587

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
configure_vnc_security() {
    local vm_name="$1"

    log_info "Configuring VNC security for VM: $vm_name"

    validate_vm_name "$vm_name" || return 1

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 588

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local vm_name="$1"

    log_info "Configuring VNC security for VM: $vm_name"

    validate_vm_name "$vm_name" || return 1

    # Security recommendations
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 589

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    log_info "Configuring VNC security for VM: $vm_name"

    validate_vm_name "$vm_name" || return 1

    # Security recommendations
    echo ""
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 590

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    log_info "Configuring VNC security for VM: $vm_name"

    validate_vm_name "$vm_name" || return 1

    # Security recommendations
    echo ""
    echo "${BLUE}VNC Security Configuration:${NC}"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 591

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    validate_vm_name "$vm_name" || return 1

    # Security recommendations
    echo ""
    echo "${BLUE}VNC Security Configuration:${NC}"
    echo "  ? Use TLS encryption for all VNC connections"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 592

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    validate_vm_name "$vm_name" || return 1

    # Security recommendations
    echo ""
    echo "${BLUE}VNC Security Configuration:${NC}"
    echo "  ? Use TLS encryption for all VNC connections"
    echo "  ? Enable SASL authentication"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 593

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    # Security recommendations
    echo ""
    echo "${BLUE}VNC Security Configuration:${NC}"
    echo "  ? Use TLS encryption for all VNC connections"
    echo "  ? Enable SASL authentication"
    echo "  ? Use strong authentication passwords"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 594

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    # Security recommendations
    echo ""
    echo "${BLUE}VNC Security Configuration:${NC}"
    echo "  ? Use TLS encryption for all VNC connections"
    echo "  ? Enable SASL authentication"
    echo "  ? Use strong authentication passwords"
    echo "  ? Restrict VNC access to known IPs"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 595

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo ""
    echo "${BLUE}VNC Security Configuration:${NC}"
    echo "  ? Use TLS encryption for all VNC connections"
    echo "  ? Enable SASL authentication"
    echo "  ? Use strong authentication passwords"
    echo "  ? Restrict VNC access to known IPs"
    echo "  ? Monitor VNC activity via audit logs"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 596

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "${BLUE}VNC Security Configuration:${NC}"
    echo "  ? Use TLS encryption for all VNC connections"
    echo "  ? Enable SASL authentication"
    echo "  ? Use strong authentication passwords"
    echo "  ? Restrict VNC access to known IPs"
    echo "  ? Monitor VNC activity via audit logs"
    echo "  ? Use firewall rules to restrict port access"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 597

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "  ? Use TLS encryption for all VNC connections"
    echo "  ? Enable SASL authentication"
    echo "  ? Use strong authentication passwords"
    echo "  ? Restrict VNC access to known IPs"
    echo "  ? Monitor VNC activity via audit logs"
    echo "  ? Use firewall rules to restrict port access"
    echo "  ? Keep VNC software updated"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 598

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "  ? Enable SASL authentication"
    echo "  ? Use strong authentication passwords"
    echo "  ? Restrict VNC access to known IPs"
    echo "  ? Monitor VNC activity via audit logs"
    echo "  ? Use firewall rules to restrict port access"
    echo "  ? Keep VNC software updated"
    echo ""
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 599

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "  ? Use strong authentication passwords"
    echo "  ? Restrict VNC access to known IPs"
    echo "  ? Monitor VNC activity via audit logs"
    echo "  ? Use firewall rules to restrict port access"
    echo "  ? Keep VNC software updated"
    echo ""

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 600

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "  ? Restrict VNC access to known IPs"
    echo "  ? Monitor VNC activity via audit logs"
    echo "  ? Use firewall rules to restrict port access"
    echo "  ? Keep VNC software updated"
    echo ""

    audit_log_entry "configure_security" "$vm_name" "success"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 601

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "  ? Monitor VNC activity via audit logs"
    echo "  ? Use firewall rules to restrict port access"
    echo "  ? Keep VNC software updated"
    echo ""

    audit_log_entry "configure_security" "$vm_name" "success"
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 602

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "  ? Use firewall rules to restrict port access"
    echo "  ? Keep VNC software updated"
    echo ""

    audit_log_entry "configure_security" "$vm_name" "success"
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 603

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "  ? Keep VNC software updated"
    echo ""

    audit_log_entry "configure_security" "$vm_name" "success"
}

generate_security_report() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 604

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo ""

    audit_log_entry "configure_security" "$vm_name" "success"
}

generate_security_report() {
    log_info "Generating VNC security report..."
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 605

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    audit_log_entry "configure_security" "$vm_name" "success"
}

generate_security_report() {
    log_info "Generating VNC security report..."

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 606

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    audit_log_entry "configure_security" "$vm_name" "success"
}

generate_security_report() {
    log_info "Generating VNC security report..."

    echo ""
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 607

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

generate_security_report() {
    log_info "Generating VNC security report..."

    echo ""
    echo "${BLUE}=== VNC/Console Security Report ===${NC}"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 608

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

generate_security_report() {
    log_info "Generating VNC security report..."

    echo ""
    echo "${BLUE}=== VNC/Console Security Report ===${NC}"
    echo ""
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 609

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
generate_security_report() {
    log_info "Generating VNC security report..."

    echo ""
    echo "${BLUE}=== VNC/Console Security Report ===${NC}"
    echo ""

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 610

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    log_info "Generating VNC security report..."

    echo ""
    echo "${BLUE}=== VNC/Console Security Report ===${NC}"
    echo ""

    echo "Configuration:"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 611

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    echo ""
    echo "${BLUE}=== VNC/Console Security Report ===${NC}"
    echo ""

    echo "Configuration:"
    echo "  Max Sessions: $MAX_CONSOLE_SESSIONS"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 612

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo ""
    echo "${BLUE}=== VNC/Console Security Report ===${NC}"
    echo ""

    echo "Configuration:"
    echo "  Max Sessions: $MAX_CONSOLE_SESSIONS"
    echo "  Token TTL: ${CONSOLE_TOKEN_TTL}s"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 613

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "${BLUE}=== VNC/Console Security Report ===${NC}"
    echo ""

    echo "Configuration:"
    echo "  Max Sessions: $MAX_CONSOLE_SESSIONS"
    echo "  Token TTL: ${CONSOLE_TOKEN_TTL}s"
    echo "  Allowed Ports: ${ALLOWED_PORTS[*]}"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 614

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo ""

    echo "Configuration:"
    echo "  Max Sessions: $MAX_CONSOLE_SESSIONS"
    echo "  Token TTL: ${CONSOLE_TOKEN_TTL}s"
    echo "  Allowed Ports: ${ALLOWED_PORTS[*]}"
    echo ""
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 615

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    echo "Configuration:"
    echo "  Max Sessions: $MAX_CONSOLE_SESSIONS"
    echo "  Token TTL: ${CONSOLE_TOKEN_TTL}s"
    echo "  Allowed Ports: ${ALLOWED_PORTS[*]}"
    echo ""

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 616

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "Configuration:"
    echo "  Max Sessions: $MAX_CONSOLE_SESSIONS"
    echo "  Token TTL: ${CONSOLE_TOKEN_TTL}s"
    echo "  Allowed Ports: ${ALLOWED_PORTS[*]}"
    echo ""

    # Check for listening VNC ports
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 617

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "  Max Sessions: $MAX_CONSOLE_SESSIONS"
    echo "  Token TTL: ${CONSOLE_TOKEN_TTL}s"
    echo "  Allowed Ports: ${ALLOWED_PORTS[*]}"
    echo ""

    # Check for listening VNC ports
    echo "Active VNC Ports:"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 618

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "  Token TTL: ${CONSOLE_TOKEN_TTL}s"
    echo "  Allowed Ports: ${ALLOWED_PORTS[*]}"
    echo ""

    # Check for listening VNC ports
    echo "Active VNC Ports:"
    netstat -tuln 2>/dev/null | grep -E '590[0-9]|600[0-9]' || echo "  None"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 619

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "  Allowed Ports: ${ALLOWED_PORTS[*]}"
    echo ""

    # Check for listening VNC ports
    echo "Active VNC Ports:"
    netstat -tuln 2>/dev/null | grep -E '590[0-9]|600[0-9]' || echo "  None"
    echo ""
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 620

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo ""

    # Check for listening VNC ports
    echo "Active VNC Ports:"
    netstat -tuln 2>/dev/null | grep -E '590[0-9]|600[0-9]' || echo "  None"
    echo ""

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 621

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    # Check for listening VNC ports
    echo "Active VNC Ports:"
    netstat -tuln 2>/dev/null | grep -E '590[0-9]|600[0-9]' || echo "  None"
    echo ""

    # Check for active sessions
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 622

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    # Check for listening VNC ports
    echo "Active VNC Ports:"
    netstat -tuln 2>/dev/null | grep -E '590[0-9]|600[0-9]' || echo "  None"
    echo ""

    # Check for active sessions
    echo "Active Console Sessions:"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 623

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "Active VNC Ports:"
    netstat -tuln 2>/dev/null | grep -E '590[0-9]|600[0-9]' || echo "  None"
    echo ""

    # Check for active sessions
    echo "Active Console Sessions:"
    if [[ -d "$STATE_DIR" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 624

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    netstat -tuln 2>/dev/null | grep -E '590[0-9]|600[0-9]' || echo "  None"
    echo ""

    # Check for active sessions
    echo "Active Console Sessions:"
    if [[ -d "$STATE_DIR" ]]; then
        local count=$(find "$STATE_DIR" -name "*.token" -type f 2>/dev/null | wc -l)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 625

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo ""

    # Check for active sessions
    echo "Active Console Sessions:"
    if [[ -d "$STATE_DIR" ]]; then
        local count=$(find "$STATE_DIR" -name "*.token" -type f 2>/dev/null | wc -l)
        echo "  Current: $count"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 626

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    # Check for active sessions
    echo "Active Console Sessions:"
    if [[ -d "$STATE_DIR" ]]; then
        local count=$(find "$STATE_DIR" -name "*.token" -type f 2>/dev/null | wc -l)
        echo "  Current: $count"
        echo "  Max: $MAX_CONSOLE_SESSIONS"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 627

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    # Check for active sessions
    echo "Active Console Sessions:"
    if [[ -d "$STATE_DIR" ]]; then
        local count=$(find "$STATE_DIR" -name "*.token" -type f 2>/dev/null | wc -l)
        echo "  Current: $count"
        echo "  Max: $MAX_CONSOLE_SESSIONS"
    else
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 628

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "Active Console Sessions:"
    if [[ -d "$STATE_DIR" ]]; then
        local count=$(find "$STATE_DIR" -name "*.token" -type f 2>/dev/null | wc -l)
        echo "  Current: $count"
        echo "  Max: $MAX_CONSOLE_SESSIONS"
    else
        echo "  Current: 0"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 629

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if [[ -d "$STATE_DIR" ]]; then
        local count=$(find "$STATE_DIR" -name "*.token" -type f 2>/dev/null | wc -l)
        echo "  Current: $count"
        echo "  Max: $MAX_CONSOLE_SESSIONS"
    else
        echo "  Current: 0"
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 630

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        local count=$(find "$STATE_DIR" -name "*.token" -type f 2>/dev/null | wc -l)
        echo "  Current: $count"
        echo "  Max: $MAX_CONSOLE_SESSIONS"
    else
        echo "  Current: 0"
    fi
    echo ""
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 631

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        echo "  Current: $count"
        echo "  Max: $MAX_CONSOLE_SESSIONS"
    else
        echo "  Current: 0"
    fi
    echo ""

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 632

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        echo "  Max: $MAX_CONSOLE_SESSIONS"
    else
        echo "  Current: 0"
    fi
    echo ""

    # Check audit log
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 633

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    else
        echo "  Current: 0"
    fi
    echo ""

    # Check audit log
    echo "Recent Audit Entries (last 10):"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 634

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        echo "  Current: 0"
    fi
    echo ""

    # Check audit log
    echo "Recent Audit Entries (last 10):"
    if [[ -f "$AUDIT_LOG" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 635

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi
    echo ""

    # Check audit log
    echo "Recent Audit Entries (last 10):"
    if [[ -f "$AUDIT_LOG" ]]; then
        tail -10 "$AUDIT_LOG"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 636

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo ""

    # Check audit log
    echo "Recent Audit Entries (last 10):"
    if [[ -f "$AUDIT_LOG" ]]; then
        tail -10 "$AUDIT_LOG"
    else
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 637

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    # Check audit log
    echo "Recent Audit Entries (last 10):"
    if [[ -f "$AUDIT_LOG" ]]; then
        tail -10 "$AUDIT_LOG"
    else
        echo "  No audit entries yet"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 638

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    # Check audit log
    echo "Recent Audit Entries (last 10):"
    if [[ -f "$AUDIT_LOG" ]]; then
        tail -10 "$AUDIT_LOG"
    else
        echo "  No audit entries yet"
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 639

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "Recent Audit Entries (last 10):"
    if [[ -f "$AUDIT_LOG" ]]; then
        tail -10 "$AUDIT_LOG"
    else
        echo "  No audit entries yet"
    fi
    echo ""
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 640

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if [[ -f "$AUDIT_LOG" ]]; then
        tail -10 "$AUDIT_LOG"
    else
        echo "  No audit entries yet"
    fi
    echo ""

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 641

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        tail -10 "$AUDIT_LOG"
    else
        echo "  No audit entries yet"
    fi
    echo ""

    # Security recommendations
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 642

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    else
        echo "  No audit entries yet"
    fi
    echo ""

    # Security recommendations
    echo "${YELLOW}Security Recommendations:${NC}"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 643

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        echo "  No audit entries yet"
    fi
    echo ""

    # Security recommendations
    echo "${YELLOW}Security Recommendations:${NC}"
    echo "  * Enable TLS for all VNC connections"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 644

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi
    echo ""

    # Security recommendations
    echo "${YELLOW}Security Recommendations:${NC}"
    echo "  * Enable TLS for all VNC connections"
    echo "  * Implement firewall rules for VNC ports"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 645

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo ""

    # Security recommendations
    echo "${YELLOW}Security Recommendations:${NC}"
    echo "  * Enable TLS for all VNC connections"
    echo "  * Implement firewall rules for VNC ports"
    echo "  * Regularly rotate authentication credentials"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 646

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    # Security recommendations
    echo "${YELLOW}Security Recommendations:${NC}"
    echo "  * Enable TLS for all VNC connections"
    echo "  * Implement firewall rules for VNC ports"
    echo "  * Regularly rotate authentication credentials"
    echo "  * Monitor audit logs for suspicious activity"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 647

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    # Security recommendations
    echo "${YELLOW}Security Recommendations:${NC}"
    echo "  * Enable TLS for all VNC connections"
    echo "  * Implement firewall rules for VNC ports"
    echo "  * Regularly rotate authentication credentials"
    echo "  * Monitor audit logs for suspicious activity"
    echo "  * Use network segmentation for VNC access"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 648

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "${YELLOW}Security Recommendations:${NC}"
    echo "  * Enable TLS for all VNC connections"
    echo "  * Implement firewall rules for VNC ports"
    echo "  * Regularly rotate authentication credentials"
    echo "  * Monitor audit logs for suspicious activity"
    echo "  * Use network segmentation for VNC access"
    echo ""
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 649

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "  * Enable TLS for all VNC connections"
    echo "  * Implement firewall rules for VNC ports"
    echo "  * Regularly rotate authentication credentials"
    echo "  * Monitor audit logs for suspicious activity"
    echo "  * Use network segmentation for VNC access"
    echo ""

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 650

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "  * Implement firewall rules for VNC ports"
    echo "  * Regularly rotate authentication credentials"
    echo "  * Monitor audit logs for suspicious activity"
    echo "  * Use network segmentation for VNC access"
    echo ""

    audit_log_entry "security_report" "system" "success"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 651

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "  * Regularly rotate authentication credentials"
    echo "  * Monitor audit logs for suspicious activity"
    echo "  * Use network segmentation for VNC access"
    echo ""

    audit_log_entry "security_report" "system" "success"
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 652

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "  * Monitor audit logs for suspicious activity"
    echo "  * Use network segmentation for VNC access"
    echo ""

    audit_log_entry "security_report" "system" "success"
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 653

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo "  * Use network segmentation for VNC access"
    echo ""

    audit_log_entry "security_report" "system" "success"
}

###############################################################################
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 654

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    echo ""

    audit_log_entry "security_report" "system" "success"
}

###############################################################################
# Main Function
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 655

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    audit_log_entry "security_report" "system" "success"
}

###############################################################################
# Main Function
###############################################################################
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 656

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    audit_log_entry "security_report" "system" "success"
}

###############################################################################
# Main Function
###############################################################################

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 657

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

###############################################################################
# Main Function
###############################################################################

main() {
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 658

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

###############################################################################
# Main Function
###############################################################################

main() {
    mkdir -p "$(dirname "$LOG_FILE")"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 659

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
###############################################################################
# Main Function
###############################################################################

main() {
    mkdir -p "$(dirname "$LOG_FILE")"
    touch "$LOG_FILE"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 660

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
# Main Function
###############################################################################

main() {
    mkdir -p "$(dirname "$LOG_FILE")"
    touch "$LOG_FILE"

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 661

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
###############################################################################

main() {
    mkdir -p "$(dirname "$LOG_FILE")"
    touch "$LOG_FILE"

    if [[ $# -lt 1 ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 662

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

main() {
    mkdir -p "$(dirname "$LOG_FILE")"
    touch "$LOG_FILE"

    if [[ $# -lt 1 ]]; then
        print_usage
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 663

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
main() {
    mkdir -p "$(dirname "$LOG_FILE")"
    touch "$LOG_FILE"

    if [[ $# -lt 1 ]]; then
        print_usage
        exit 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 664

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    mkdir -p "$(dirname "$LOG_FILE")"
    touch "$LOG_FILE"

    if [[ $# -lt 1 ]]; then
        print_usage
        exit 1
    fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 665

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    touch "$LOG_FILE"

    if [[ $# -lt 1 ]]; then
        print_usage
        exit 1
    fi

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 666

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    if [[ $# -lt 1 ]]; then
        print_usage
        exit 1
    fi

    local command="$1"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 667

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    if [[ $# -lt 1 ]]; then
        print_usage
        exit 1
    fi

    local command="$1"
    shift
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 668

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        print_usage
        exit 1
    fi

    local command="$1"
    shift

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 669

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        exit 1
    fi

    local command="$1"
    shift

    case "$command" in
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 670

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    fi

    local command="$1"
    shift

    case "$command" in
        ensure-port)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 671

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    local command="$1"
    shift

    case "$command" in
        ensure-port)
            ensure_vnc_port "$@"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 672

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    local command="$1"
    shift

    case "$command" in
        ensure-port)
            ensure_vnc_port "$@"
            ;;
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 673

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    shift

    case "$command" in
        ensure-port)
            ensure_vnc_port "$@"
            ;;
        generate-token)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 674

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

    case "$command" in
        ensure-port)
            ensure_vnc_port "$@"
            ;;
        generate-token)
            local vm_name="${1:-}"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 675

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    case "$command" in
        ensure-port)
            ensure_vnc_port "$@"
            ;;
        generate-token)
            local vm_name="${1:-}"
            local token_type="${2:-read-write}"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 676

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        ensure-port)
            ensure_vnc_port "$@"
            ;;
        generate-token)
            local vm_name="${1:-}"
            local token_type="${2:-read-write}"
            if [[ -z "$vm_name" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 677

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            ensure_vnc_port "$@"
            ;;
        generate-token)
            local vm_name="${1:-}"
            local token_type="${2:-read-write}"
            if [[ -z "$vm_name" ]]; then
                log_error "VM name required"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 678

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            ;;
        generate-token)
            local vm_name="${1:-}"
            local token_type="${2:-read-write}"
            if [[ -z "$vm_name" ]]; then
                log_error "VM name required"
                exit 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 679

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        generate-token)
            local vm_name="${1:-}"
            local token_type="${2:-read-write}"
            if [[ -z "$vm_name" ]]; then
                log_error "VM name required"
                exit 1
            fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 680

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            local vm_name="${1:-}"
            local token_type="${2:-read-write}"
            if [[ -z "$vm_name" ]]; then
                log_error "VM name required"
                exit 1
            fi
            generate_console_token "$vm_name" "$token_type"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 681

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            local token_type="${2:-read-write}"
            if [[ -z "$vm_name" ]]; then
                log_error "VM name required"
                exit 1
            fi
            generate_console_token "$vm_name" "$token_type"
            ;;
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 682

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            if [[ -z "$vm_name" ]]; then
                log_error "VM name required"
                exit 1
            fi
            generate_console_token "$vm_name" "$token_type"
            ;;
        validate-vnc)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 683

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
                log_error "VM name required"
                exit 1
            fi
            generate_console_token "$vm_name" "$token_type"
            ;;
        validate-vnc)
            local host_port="${1:-}"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 684

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
                exit 1
            fi
            generate_console_token "$vm_name" "$token_type"
            ;;
        validate-vnc)
            local host_port="${1:-}"
            if [[ -z "$host_port" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 685

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            fi
            generate_console_token "$vm_name" "$token_type"
            ;;
        validate-vnc)
            local host_port="${1:-}"
            if [[ -z "$host_port" ]]; then
                log_error "host:port required"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 686

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            generate_console_token "$vm_name" "$token_type"
            ;;
        validate-vnc)
            local host_port="${1:-}"
            if [[ -z "$host_port" ]]; then
                log_error "host:port required"
                exit 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 687

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            ;;
        validate-vnc)
            local host_port="${1:-}"
            if [[ -z "$host_port" ]]; then
                log_error "host:port required"
                exit 1
            fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 688

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        validate-vnc)
            local host_port="${1:-}"
            if [[ -z "$host_port" ]]; then
                log_error "host:port required"
                exit 1
            fi
            validate_vnc_reachable "$host_port"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 689

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            local host_port="${1:-}"
            if [[ -z "$host_port" ]]; then
                log_error "host:port required"
                exit 1
            fi
            validate_vnc_reachable "$host_port"
            ;;
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 690

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            if [[ -z "$host_port" ]]; then
                log_error "host:port required"
                exit 1
            fi
            validate_vnc_reachable "$host_port"
            ;;
        list-sessions)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 691

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
                log_error "host:port required"
                exit 1
            fi
            validate_vnc_reachable "$host_port"
            ;;
        list-sessions)
            list_console_sessions
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 692

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
                exit 1
            fi
            validate_vnc_reachable "$host_port"
            ;;
        list-sessions)
            list_console_sessions
            ;;
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 693

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            fi
            validate_vnc_reachable "$host_port"
            ;;
        list-sessions)
            list_console_sessions
            ;;
        close-session)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 694

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            validate_vnc_reachable "$host_port"
            ;;
        list-sessions)
            list_console_sessions
            ;;
        close-session)
            local token="${1:-}"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 695

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            ;;
        list-sessions)
            list_console_sessions
            ;;
        close-session)
            local token="${1:-}"
            if [[ -z "$token" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 696

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        list-sessions)
            list_console_sessions
            ;;
        close-session)
            local token="${1:-}"
            if [[ -z "$token" ]]; then
                log_error "Token required"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 697

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            list_console_sessions
            ;;
        close-session)
            local token="${1:-}"
            if [[ -z "$token" ]]; then
                log_error "Token required"
                exit 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 698

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            ;;
        close-session)
            local token="${1:-}"
            if [[ -z "$token" ]]; then
                log_error "Token required"
                exit 1
            fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 699

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        close-session)
            local token="${1:-}"
            if [[ -z "$token" ]]; then
                log_error "Token required"
                exit 1
            fi
            close_console_session "$token"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 700

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            local token="${1:-}"
            if [[ -z "$token" ]]; then
                log_error "Token required"
                exit 1
            fi
            close_console_session "$token"
            ;;
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 701

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            if [[ -z "$token" ]]; then
                log_error "Token required"
                exit 1
            fi
            close_console_session "$token"
            ;;
        config)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 702

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
                log_error "Token required"
                exit 1
            fi
            close_console_session "$token"
            ;;
        config)
            configure_vnc_security "$@"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 703

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
                exit 1
            fi
            close_console_session "$token"
            ;;
        config)
            configure_vnc_security "$@"
            ;;
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 704

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            fi
            close_console_session "$token"
            ;;
        config)
            configure_vnc_security "$@"
            ;;
        security-report)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 705

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            close_console_session "$token"
            ;;
        config)
            configure_vnc_security "$@"
            ;;
        security-report)
            generate_security_report
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 706

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            ;;
        config)
            configure_vnc_security "$@"
            ;;
        security-report)
            generate_security_report
            ;;
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 707

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        config)
            configure_vnc_security "$@"
            ;;
        security-report)
            generate_security_report
            ;;
        --help|-h)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 708

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            configure_vnc_security "$@"
            ;;
        security-report)
            generate_security_report
            ;;
        --help|-h)
            print_usage
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 709

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            ;;
        security-report)
            generate_security_report
            ;;
        --help|-h)
            print_usage
            exit 0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 710

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        security-report)
            generate_security_report
            ;;
        --help|-h)
            print_usage
            exit 0
            ;;
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 711

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            generate_security_report
            ;;
        --help|-h)
            print_usage
            exit 0
            ;;
        --version)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 712

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            ;;
        --help|-h)
            print_usage
            exit 0
            ;;
        --version)
            print_version
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 713

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        --help|-h)
            print_usage
            exit 0
            ;;
        --version)
            print_version
            exit 0
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 714

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            print_usage
            exit 0
            ;;
        --version)
            print_version
            exit 0
            ;;
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 715

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            exit 0
            ;;
        --version)
            print_version
            exit 0
            ;;
        *)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 716

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            ;;
        --version)
            print_version
            exit 0
            ;;
        *)
            log_error "Unknown command: $command"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 717

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        --version)
            print_version
            exit 0
            ;;
        *)
            log_error "Unknown command: $command"
            print_usage
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 718

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            print_version
            exit 0
            ;;
        *)
            log_error "Unknown command: $command"
            print_usage
            exit 1
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 719

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            exit 0
            ;;
        *)
            log_error "Unknown command: $command"
            print_usage
            exit 1
            ;;
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 720

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            ;;
        *)
            log_error "Unknown command: $command"
            print_usage
            exit 1
            ;;
    esac
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 721

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        *)
            log_error "Unknown command: $command"
            print_usage
            exit 1
            ;;
    esac
}
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 722

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            log_error "Unknown command: $command"
            print_usage
            exit 1
            ;;
    esac
}

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 723

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            print_usage
            exit 1
            ;;
    esac
}

while [[ $# -gt 0 ]]; do
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 724

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            exit 1
            ;;
    esac
}

while [[ $# -gt 0 ]]; do
    case "$1" in
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 725

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            ;;
    esac
}

while [[ $# -gt 0 ]]; do
    case "$1" in
        --dry-run)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 726

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    esac
}

while [[ $# -gt 0 ]]; do
    case "$1" in
        --dry-run)
            DRY_RUN="true"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 727

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
}

while [[ $# -gt 0 ]]; do
    case "$1" in
        --dry-run)
            DRY_RUN="true"
            shift
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 728

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

while [[ $# -gt 0 ]]; do
    case "$1" in
        --dry-run)
            DRY_RUN="true"
            shift
            ;;
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 729

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
while [[ $# -gt 0 ]]; do
    case "$1" in
        --dry-run)
            DRY_RUN="true"
            shift
            ;;
        --verbose)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 730

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    case "$1" in
        --dry-run)
            DRY_RUN="true"
            shift
            ;;
        --verbose)
            VERBOSE="true"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 731

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        --dry-run)
            DRY_RUN="true"
            shift
            ;;
        --verbose)
            VERBOSE="true"
            shift
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 732

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            DRY_RUN="true"
            shift
            ;;
        --verbose)
            VERBOSE="true"
            shift
            ;;
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 733

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            shift
            ;;
        --verbose)
            VERBOSE="true"
            shift
            ;;
        --json)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 734

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            ;;
        --verbose)
            VERBOSE="true"
            shift
            ;;
        --json)
            JSON_OUTPUT="true"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 735

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        --verbose)
            VERBOSE="true"
            shift
            ;;
        --json)
            JSON_OUTPUT="true"
            shift
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 736

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            VERBOSE="true"
            shift
            ;;
        --json)
            JSON_OUTPUT="true"
            shift
            ;;
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 737

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            shift
            ;;
        --json)
            JSON_OUTPUT="true"
            shift
            ;;
        *)
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 738

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            ;;
        --json)
            JSON_OUTPUT="true"
            shift
            ;;
        *)
            break
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 739

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        --json)
            JSON_OUTPUT="true"
            shift
            ;;
        *)
            break
            ;;
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 740

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            JSON_OUTPUT="true"
            shift
            ;;
        *)
            break
            ;;
    esac
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 741

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            shift
            ;;
        *)
            break
            ;;
    esac
done
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 742

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            ;;
        *)
            break
            ;;
    esac
done

```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 743

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
        *)
            break
            ;;
    esac
done

if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 744

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            break
            ;;
    esac
done

if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
    main "$@"
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 745

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
            ;;
    esac
done

if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
    main "$@"
fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 746

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
    esac
done

if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
    main "$@"
fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 747

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```
done

if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
    main "$@"
fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 748

**Tool:** shellcheck | **Code:** `1017` | **Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

**Context:**
```

if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
    main "$@"
fi
```

**Proposal:**
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

## Implementation Progress

To mark an issue as fixed, add the issue code to the line below with a ✅ emoji:

**Fixed Issues:** (none yet)

---
*Updated: (auto-populated by coding expert)*
