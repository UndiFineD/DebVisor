# Code Issues Report: etc\debvisor\validate-blocklists.sh

Generated: 2025-12-13T17:07:06.957463
Source: etc\debvisor\validate-blocklists.sh

## Issues Summary

Total: 140 issues found

| Line | Column | Tool | Code | Severity | Message |
|------|--------|------|------|----------|---------|
| 1 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 2 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 3 | 66 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 4 | 67 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 5 | 42 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 6 | 49 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 7 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 8 | 68 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 9 | 75 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 10 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 11 | 33 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 12 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 13 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 14 | 93 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 15 | 112 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 16 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 17 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 18 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 19 | 14 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 20 | 21 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 21 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 22 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 23 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 24 | 12 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 25 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 26 | 20 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 27 | 17 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 28 | 19 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 29 | 20 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 30 | 24 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 31 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 32 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 33 | 38 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 34 | 25 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 35 | 5 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 36 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 37 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 38 | 16 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 39 | 38 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 40 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 41 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 42 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 43 | 40 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 44 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 45 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 46 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 47 | 37 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 48 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 49 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 50 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 51 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 52 | 72 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 53 | 46 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 54 | 109 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 55 | 47 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 56 | 108 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 57 | 45 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 58 | 129 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 59 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 60 | 100 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 61 | 13 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 62 | 5 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 63 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 64 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 65 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 66 | 18 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 67 | 23 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 68 | 19 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 69 | 22 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 70 | 22 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 71 | 29 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 72 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 73 | 47 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 74 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 75 | 30 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 76 | 51 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 77 | 13 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 78 | 5 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 79 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 80 | 43 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 81 | 30 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 82 | 19 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 83 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 84 | 36 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 85 | 62 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 86 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 87 | 60 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 88 | 56 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 89 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 90 | 31 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 91 | 15 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 92 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 93 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 94 | 22 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 95 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 96 | 27 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 97 | 50 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 98 | 70 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 99 | 24 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 100 | 22 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 101 | 9 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 102 | 63 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 103 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 104 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 105 | 34 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 106 | 63 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 107 | 68 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 108 | 9 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 109 | 36 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 110 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 111 | 17 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 112 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 113 | 72 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 114 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 115 | 36 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 116 | 67 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 117 | 13 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 118 | 7 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 119 | 67 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 120 | 13 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 121 | 5 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 122 | 2 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 123 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 124 | 19 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 125 | 23 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 126 | 23 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 127 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 128 | 53 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 129 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 130 | 29 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 131 | 122 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 132 | 122 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 133 | 1 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 134 | 46 | shellcheck | `1017` | ERROR | Literal carriage return. Run script through tr -d '\r' . |
| 135 | 3 | shellcheck | `1009` | INFO | The mentioned syntax error was in this simple command. |
| 135 | 11 | shellcheck | `1073` | ERROR | Couldn't parse this here document. Fix to allow more checks. |
| 135 | 14 | shellcheck | `1041` | ERROR | Found ''EOF'\r' further down, but not on a separate line. |
| 135 | 14 | shellcheck | `1042` | ERROR | Close matches include '  python &2

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 32

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

YELLOW='\033[1;33m'
NC='\033[0m' # No Color

print_verbose() {
  if [["$VERBOSE" == "true"]]; then

```python
echo "[INFO] $*" >&2
```python

  fi

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 33

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

NC='\033[0m' # No Color

print_verbose() {
  if [["$VERBOSE" == "true"]]; then

```python
echo "[INFO] $*" >&2
```python

  fi
}

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 34

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

print_verbose() {
  if [["$VERBOSE" == "true"]]; then

```python
echo "[INFO] $*" >&2
```python

  fi
}

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 35

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

print_verbose() {
  if [["$VERBOSE" == "true"]]; then

```python
echo "[INFO] $*" >&2
```python

  fi
}

print_error() {

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 36

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  if [["$VERBOSE" == "true"]]; then

```python
echo "[INFO] $*" >&2
```python

  fi
}

print_error() {
  echo -e "${RED}[ERROR]${NC} $*" >&2

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 37

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
echo "[INFO] $*" >&2
```python

  fi
}

print_error() {
  echo -e "${RED}[ERROR]${NC} $*" >&2
}

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 38

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  fi
}

print_error() {
  echo -e "${RED}[ERROR]${NC} $*" >&2
}

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 39

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

}

print_error() {
  echo -e "${RED}[ERROR]${NC} $*" >&2
}

print_warning() {

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 40

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

print_error() {
  echo -e "${RED}[ERROR]${NC} $*" >&2
}

print_warning() {
  echo -e "${YELLOW}[WARN]${NC} $*" >&2

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 41

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

print_error() {
  echo -e "${RED}[ERROR]${NC} $*" >&2
}

print_warning() {
  echo -e "${YELLOW}[WARN]${NC} $*" >&2
}

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 42

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  echo -e "${RED}[ERROR]${NC} $*" >&2
}

print_warning() {
  echo -e "${YELLOW}[WARN]${NC} $*" >&2
}

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 43

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

}

print_warning() {
  echo -e "${YELLOW}[WARN]${NC} $*" >&2
}

print_success() {

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 44

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

print_warning() {
  echo -e "${YELLOW}[WARN]${NC} $*" >&2
}

print_success() {
  echo -e "${GREEN}[OK]${NC} $*" >&2

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 45

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

print_warning() {
  echo -e "${YELLOW}[WARN]${NC} $*" >&2
}

print_success() {
  echo -e "${GREEN}[OK]${NC} $*" >&2
}

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 46

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  echo -e "${YELLOW}[WARN]${NC} $*" >&2
}

print_success() {
  echo -e "${GREEN}[OK]${NC} $*" >&2
}

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 47

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

}

print_success() {
  echo -e "${GREEN}[OK]${NC} $*" >&2
}

validate_cidr() {

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 48

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

print_success() {
  echo -e "${GREEN}[OK]${NC} $*" >&2
}

validate_cidr() {
  local cidr="$1"

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 49

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

print_success() {
  echo -e "${GREEN}[OK]${NC} $*" >&2
}

validate_cidr() {
  local cidr="$1"
  # Prefer python3, fallback to python, then PowerShell/.NET on Windows

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 50

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  echo -e "${GREEN}[OK]${NC} $*" >&2
}

validate_cidr() {
  local cidr="$1"
  # Prefer python3, fallback to python, then PowerShell/.NET on Windows
  if command -v python3 >/dev/null 2>&1; then

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 51

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

}

validate_cidr() {
  local cidr="$1"
  # Prefer python3, fallback to python, then PowerShell/.NET on Windows
  if command -v python3 >/dev/null 2>&1; then

```python
python3 -c "from ipaddress import ip_network; ip_network('$cidr', strict=False)" 2>/dev/null || return 1
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 52

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

validate_cidr() {
  local cidr="$1"
  # Prefer python3, fallback to python, then PowerShell/.NET on Windows
  if command -v python3 >/dev/null 2>&1; then

```python
python3 -c "from ipaddress import ip_network; ip_network('$cidr', strict=False)" 2>/dev/null || return 1
```python

  elif command -v python >/dev/null 2>&1; then

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 53

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

validate_cidr() {
  local cidr="$1"
  # Prefer python3, fallback to python, then PowerShell/.NET on Windows
  if command -v python3 >/dev/null 2>&1; then

```python
python3 -c "from ipaddress import ip_network; ip_network('$cidr', strict=False)" 2>/dev/null || return 1
```python

  elif command -v python >/dev/null 2>&1; then

```python
python -c "from ipaddress import ip_network; ip_network('$cidr', strict=False)" 2>/dev/null || return 1
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 54

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  local cidr="$1"
  # Prefer python3, fallback to python, then PowerShell/.NET on Windows
  if command -v python3 >/dev/null 2>&1; then

```python
python3 -c "from ipaddress import ip_network; ip_network('$cidr', strict=False)" 2>/dev/null || return 1
```python

  elif command -v python >/dev/null 2>&1; then

```python
python -c "from ipaddress import ip_network; ip_network('$cidr', strict=False)" 2>/dev/null || return 1
```python

  elif command -v pwsh >/dev/null 2>&1; then

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 55

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  # Prefer python3, fallback to python, then PowerShell/.NET on Windows
  if command -v python3 >/dev/null 2>&1; then

```python
python3 -c "from ipaddress import ip_network; ip_network('$cidr', strict=False)" 2>/dev/null || return 1
```python

  elif command -v python >/dev/null 2>&1; then

```python
python -c "from ipaddress import ip_network; ip_network('$cidr', strict=False)" 2>/dev/null || return 1
```python

  elif command -v pwsh >/dev/null 2>&1; then

```python
pwsh -NoProfile -Command "try { [void][System.Net.IPAddress]::Parse(('$cidr').Split[0]('/')) } catch { exit 1 }" || return 1
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 56

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  if command -v python3 >/dev/null 2>&1; then

```python
python3 -c "from ipaddress import ip_network; ip_network('$cidr', strict=False)" 2>/dev/null || return 1
```python

  elif command -v python >/dev/null 2>&1; then

```python
python -c "from ipaddress import ip_network; ip_network('$cidr', strict=False)" 2>/dev/null || return 1
```python

  elif command -v pwsh >/dev/null 2>&1; then

```python
pwsh -NoProfile -Command "try { [void][System.Net.IPAddress]::Parse(('$cidr').Split[0]('/')) } catch { exit 1 }" || return 1
```python

  else

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 57

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
python3 -c "from ipaddress import ip_network; ip_network('$cidr', strict=False)" 2>/dev/null || return 1
```python

  elif command -v python >/dev/null 2>&1; then

```python
python -c "from ipaddress import ip_network; ip_network('$cidr', strict=False)" 2>/dev/null || return 1
```python

  elif command -v pwsh >/dev/null 2>&1; then

```python
pwsh -NoProfile -Command "try { [void][System.Net.IPAddress]::Parse(('$cidr').Split[0]('/')) } catch { exit 1 }" || return 1
```python

  else

```python
print_error "No validator runtime found (python3/python/pwsh). Install Python 3 or PowerShell."
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 58

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  elif command -v python >/dev/null 2>&1; then

```python
python -c "from ipaddress import ip_network; ip_network('$cidr', strict=False)" 2>/dev/null || return 1
```python

  elif command -v pwsh >/dev/null 2>&1; then

```python
pwsh -NoProfile -Command "try { [void][System.Net.IPAddress]::Parse(('$cidr').Split[0]('/')) } catch { exit 1 }" || return 1
```python

  else

```python
print_error "No validator runtime found (python3/python/pwsh). Install Python 3 or PowerShell."
return 1
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 59

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
python -c "from ipaddress import ip_network; ip_network('$cidr', strict=False)" 2>/dev/null || return 1
```python

  elif command -v pwsh >/dev/null 2>&1; then

```python
pwsh -NoProfile -Command "try { [void][System.Net.IPAddress]::Parse(('$cidr').Split[0]('/')) } catch { exit 1 }" || return 1
```python

  else

```python
print_error "No validator runtime found (python3/python/pwsh). Install Python 3 or PowerShell."
return 1
```python

  fi

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 60

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  elif command -v pwsh >/dev/null 2>&1; then

```python
pwsh -NoProfile -Command "try { [void][System.Net.IPAddress]::Parse(('$cidr').Split[0]('/')) } catch { exit 1 }" || return 1
```python

  else

```python
print_error "No validator runtime found (python3/python/pwsh). Install Python 3 or PowerShell."
return 1
```python

  fi
}

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 61

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
pwsh -NoProfile -Command "try { [void][System.Net.IPAddress]::Parse(('$cidr').Split[0]('/')) } catch { exit 1 }" || return 1
```python

  else

```python
print_error "No validator runtime found (python3/python/pwsh). Install Python 3 or PowerShell."
return 1
```python

  fi
}

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 62

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  else

```python
print_error "No validator runtime found (python3/python/pwsh). Install Python 3 or PowerShell."
return 1
```python

  fi
}

validate_file() {

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 63

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
print_error "No validator runtime found (python3/python/pwsh). Install Python 3 or PowerShell."
return 1
```python

  fi
}

validate_file() {
  local file="$1"

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 64

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
return 1
```python

  fi
}

validate_file() {
  local file="$1"
  local file_type="$2"

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 65

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  fi
}

validate_file() {
  local file="$1"
  local file_type="$2"
  local line_num=0

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 66

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

}

validate_file() {
  local file="$1"
  local file_type="$2"
  local line_num=0
  local entry_count=0

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 67

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

validate_file() {
  local file="$1"
  local file_type="$2"
  local line_num=0
  local entry_count=0
  local error_count=0

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 68

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

validate_file() {
  local file="$1"
  local file_type="$2"
  local line_num=0
  local entry_count=0
  local error_count=0
  local duplicate_entries=()

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 69

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  local file="$1"
  local file_type="$2"
  local line_num=0
  local entry_count=0
  local error_count=0
  local duplicate_entries=()

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 70

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  local file_type="$2"
  local line_num=0
  local entry_count=0
  local error_count=0
  local duplicate_entries=()

  print_verbose "Validating $file_type: $file"

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 71

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  local line_num=0
  local entry_count=0
  local error_count=0
  local duplicate_entries=()

  print_verbose "Validating $file_type: $file"

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 72

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  local entry_count=0
  local error_count=0
  local duplicate_entries=()

  print_verbose "Validating $file_type: $file"

  if [[! -f "$file"]]; then

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 73

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  local error_count=0
  local duplicate_entries=()

  print_verbose "Validating $file_type: $file"

  if [[! -f "$file"]]; then

```python
print_error "$file_type file not found: $file"
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 74

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  local duplicate_entries=()

  print_verbose "Validating $file_type: $file"

  if [[! -f "$file"]]; then

```python
print_error "$file_type file not found: $file"
return 1
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 75

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  print_verbose "Validating $file_type: $file"

  if [[! -f "$file"]]; then

```python
print_error "$file_type file not found: $file"
return 1
```python

  fi

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 76

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  print_verbose "Validating $file_type: $file"

  if [[! -f "$file"]]; then

```python
print_error "$file_type file not found: $file"
return 1
```python

  fi

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 77

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  if [[! -f "$file"]]; then

```python
print_error "$file_type file not found: $file"
return 1
```python

  fi

  # Read file and validate each CIDR entry

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 78

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  if [[! -f "$file"]]; then

```python
print_error "$file_type file not found: $file"
return 1
```python

  fi

  # Read file and validate each CIDR entry
  while IFS= read -r line; do

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 79

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
print_error "$file_type file not found: $file"
return 1
```python

  fi

  # Read file and validate each CIDR entry
  while IFS= read -r line; do

```python
((line_num++))
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 80

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
return 1
```python

  fi

  # Read file and validate each CIDR entry
  while IFS= read -r line; do

```python
((line_num++))

```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 81

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  fi

  # Read file and validate each CIDR entry
  while IFS= read -r line; do

```python
((line_num++))

## Skip empty lines and comments
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 82

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  # Read file and validate each CIDR entry
  while IFS= read -r line; do

```python
((line_num++))

## Skip empty lines and comments
[[-z "$line" || "$line" =~ ^[[:space:]]*#]] && continue
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 83

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  # Read file and validate each CIDR entry
  while IFS= read -r line; do

```python
((line_num++))

## Skip empty lines and comments
[[-z "$line" || "$line" =~ ^[[:space:]]*#]] && continue

```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 84

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  while IFS= read -r line; do

```python
((line_num++))

## Skip empty lines and comments
[[-z "$line" || "$line" =~ ^[[:space:]]*#]] && continue

## Extract CIDR entry (before inline comment if present)
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 85

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
((line_num++))

## Skip empty lines and comments
[[-z "$line" || "$line" =~ ^[[:space:]]*#]] && continue

## Extract CIDR entry (before inline comment if present)
local entry=$(echo "$line" | sed 's/#.*//' | xargs)
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 86

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
## Skip empty lines and comments
[[-z "$line" || "$line" =~ ^[[:space:]]*#]] && continue

## Extract CIDR entry (before inline comment if present)
local entry=$(echo "$line" | sed 's/#.*//' | xargs)

```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 87

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
## Skip empty lines and comments
[[-z "$line" || "$line" =~ ^[[:space:]]*#]] && continue

## Extract CIDR entry (before inline comment if present)
local entry=$(echo "$line" | sed 's/#.*//' | xargs)

if [[-z "$entry"]]; then
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 88

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
[[-z "$line" || "$line" =~ ^[[:space:]]*#]] && continue

## Extract CIDR entry (before inline comment if present)
local entry=$(echo "$line" | sed 's/#.*//' | xargs)

if [[-z "$entry"]]; then
  continue
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 89

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
## Extract CIDR entry (before inline comment if present)
local entry=$(echo "$line" | sed 's/#.*//' | xargs)

if [[-z "$entry"]]; then
  continue
fi
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 90

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
## Extract CIDR entry (before inline comment if present)
local entry=$(echo "$line" | sed 's/#.*//' | xargs)

if [[-z "$entry"]]; then
  continue
fi

```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 91

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
local entry=$(echo "$line" | sed 's/#.*//' | xargs)

if [[-z "$entry"]]; then
  continue
fi

((entry_count++))
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 92

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
if [[-z "$entry"]]; then
  continue
fi

((entry_count++))

```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 93

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
if [[-z "$entry"]]; then
  continue
fi

((entry_count++))

## Validate CIDR format
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 94

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
  continue
fi

((entry_count++))

## Validate CIDR format
if ! validate_cidr "$entry" 2>/dev/null; then
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 95

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
fi

((entry_count++))

## Validate CIDR format
if ! validate_cidr "$entry" 2>/dev/null; then
  print_error "$file_type:$line_num: Invalid CIDR format: $entry"
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 96

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
((entry_count++))

## Validate CIDR format
if ! validate_cidr "$entry" 2>/dev/null; then
  print_error "$file_type:$line_num: Invalid CIDR format: $entry"
  ((error_count++))
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 97

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
((entry_count++))

## Validate CIDR format
if ! validate_cidr "$entry" 2>/dev/null; then
  print_error "$file_type:$line_num: Invalid CIDR format: $entry"
  ((error_count++))
  ((EXIT_CODE++))
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 98

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
## Validate CIDR format
if ! validate_cidr "$entry" 2>/dev/null; then
  print_error "$file_type:$line_num: Invalid CIDR format: $entry"
  ((error_count++))
  ((EXIT_CODE++))
else
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 99

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
## Validate CIDR format
if ! validate_cidr "$entry" 2>/dev/null; then
  print_error "$file_type:$line_num: Invalid CIDR format: $entry"
  ((error_count++))
  ((EXIT_CODE++))
else
  print_verbose "$file_type:$line_num: Valid CIDR: $entry"
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 100

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
if ! validate_cidr "$entry" 2>/dev/null; then
  print_error "$file_type:$line_num: Invalid CIDR format: $entry"
  ((error_count++))
  ((EXIT_CODE++))
else
  print_verbose "$file_type:$line_num: Valid CIDR: $entry"
fi
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 101

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
  print_error "$file_type:$line_num: Invalid CIDR format: $entry"
  ((error_count++))
  ((EXIT_CODE++))
else
  print_verbose "$file_type:$line_num: Valid CIDR: $entry"
fi

```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 102

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
  ((error_count++))
  ((EXIT_CODE++))
else
  print_verbose "$file_type:$line_num: Valid CIDR: $entry"
fi

## Check for duplicate entries
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 103

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
  ((EXIT_CODE++))
else
  print_verbose "$file_type:$line_num: Valid CIDR: $entry"
fi

## Check for duplicate entries
if [[" ${duplicate_entries[*]} " == *" $entry "*]]; then
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 104

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
else
  print_verbose "$file_type:$line_num: Valid CIDR: $entry"
fi

## Check for duplicate entries
if [[" ${duplicate_entries[*]} " == *" $entry "*]]; then
  print_warning "$file_type:$line_num: Duplicate entry: $entry"
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 105

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
  print_verbose "$file_type:$line_num: Valid CIDR: $entry"
fi

## Check for duplicate entries
if [[" ${duplicate_entries[*]} " == *" $entry "*]]; then
  print_warning "$file_type:$line_num: Duplicate entry: $entry"
else
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 106

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
fi

## Check for duplicate entries
if [[" ${duplicate_entries[*]} " == *" $entry "*]]; then
  print_warning "$file_type:$line_num: Duplicate entry: $entry"
else
  duplicate_entries+=("$entry")
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 107

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
## Check for duplicate entries
if [[" ${duplicate_entries[*]} " == *" $entry "*]]; then
  print_warning "$file_type:$line_num: Duplicate entry: $entry"
else
  duplicate_entries+=("$entry")
fi
```python

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 108

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

```python
## Check for duplicate entries
if [[" ${duplicate_entries[*]} " == *" $entry "*]]; then
  print_warning "$file_type:$line_num: Duplicate entry: $entry"
else
  duplicate_entries+=("$entry")
fi
```python

  done /dev/null 2>&1; then

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 132

**Tool:**shellcheck |**Code:**`1017` |**Severity:** ERROR

**Message:** Literal carriage return. Run script through tr -d '\r' .

### Context
```python

  # Extract all CIDR entries
  local block_entries=$(grep -v '^[[:space:]]*$' "$blocklist" | grep -v '^[[:space:]]*#' | sed 's/#.*//' | xargs || true)
  local white_entries=$(grep -v '^[[:space:]]*$' "$whitelist" | grep -v '^[[:space:]]*#' | sed 's/#.*//' | xargs || true)

  if command -v python3 >/dev/null 2>&1; then
  python3 /dev/null 2>&1; then
  python3 /dev/null 2>&1; then
  python3 /dev/null 2>&1; then
  python3 /dev/null 2>&1; then
  python3 /dev/null 2>&1; then
  python3 /dev/null 2>&1; then
  python3 /dev/null 2>&1; then
  python3 << 'EOF'
import sys
from ipaddress import ip_network, collapse_addresses

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

### Issue at Line 342

**Tool:**shellcheck |**Code:**`1072` |**Severity:** ERROR

**Message:** Here document was not correctly terminated. Fix any mentioned problems and try again.

### Context
```python

  print_error "Validation failed with $EXIT_CODE errors"
  exit 1
fi

```python

### Proposal
- Review the issue message above
- Consider the context code
- Apply the appropriate fix (e.g., fix linting error, add type hints, improve security)
- Ensure the fix aligns with the codebase style and the context.md guidelines

---

## Implementation Progress

To mark an issue as fixed, add the issue code to the line below with a ✅ emoji:

**Fixed Issues:** (none yet)

---
*Updated: (auto-populated by coding expert)*
